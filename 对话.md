# 对话记录

## 2026-05-18 12:12 CST — Vercel 快速演示部署

### 用户原始需求

用户要求实施“Vercel 快速演示部署计划”：初始化 Git，创建 GitHub 仓库，部署到 Vercel，配置已有 API Key，保证 `/workspace` 主功能可公开访问；用户随后追加“继续部署”。

### 本轮处理

- 初始化本地 Git 仓库，设置中文提交信息，推送到 GitHub：`https://github.com/byxacb/haikesong-cross-border-guide`。
- 增加 `.vercelignore`，排除 `.env*`、`.next/`、`node_modules/`、`.npm-cache/`、`.playwright-cli/`、`.qoder/repowiki/`、`outputs/` 和 `*.pptx`，避免部署上传密钥、本地构建缓存和交付物。
- 创建 Vercel 项目 `humdeci/haikesong-cross-border-guide`，关闭 SSO 部署保护。
- 通过 Vercel Production 环境变量配置 `DEEPSEEK_API_KEY`、`BRAVE_API_KEY`、`BRAVE_SEARCH_API_KEY`；未配置 `ZHIPU_API_KEY`，本轮也不新增文生图能力。
- 第一次普通 CLI 部署因为项目 framework 识别为 `Other` 且 SSO 保护开启，公网访问出现认证/404；随后通过 API 将 framework 修正为 `nextjs`，改用 `vercel build --prod` + `vercel deploy --prebuilt --prod`，成功部署预构建产物。

### 验证证据

- `npm run lint`：通过。
- `npm run build`：通过。
- Vercel 部署：`dpl_9F98xLzR8aKTWfMaRR4Pg6EBrDnH`，状态 `READY`。
- 生产地址：`https://haikesong-cross-border-guide.vercel.app/workspace`。
- `curl -I https://haikesong-cross-border-guide.vercel.app/workspace`：HTTP 200。
- 页面 HTML 包含 `跨境开店注册助手`、`开始注册引导`；不包含 `DeepSeek`、`Tavily`、`Brave`、`搜索 API Key`、`自适应主控`、`动态问诊`。
- `POST /api/ai/workflow-step`：HTTP 200，`success:true`，返回“你想注册的经营主体类型是？”首问。
- Playwright 打开生产 `/workspace`，点击“开始注册引导”后进入首问；控制台 0 error / 0 warning。
- `POST /api/ai/workflow-final-plan` 上海跨境电商公司场景：HTTP 200，`success:true`，返回 11 步路线图、11 组材料、10 个官方入口卡。

### 遗留说明

- Vercel 快速演示地址不能承诺中国大陆正式稳定访问；如果比赛现场网络访问不稳，应改走国内云服务 + ICP 备案或至少准备本地演示兜底。
- Vercel GitHub App 未成功连接新仓库，后续自动部署可能需要在 Vercel 控制台授权 GitHub 仓库；本次已通过 CLI 预构建部署完成。
- 根目录 `跨境开店注册助手-两分钟介绍.pptx` 是 PPT 交付物，未提交到 GitHub，也不会被 Vercel 上传。

## 2026-05-18 11:09 CST — 制作两分钟项目介绍 PPT

### 用户原始需求

用户要求执行“两分钟项目介绍 PPT 制作计划”：制作 5 页中文 16:9 可编辑 PowerPoint，介绍“跨境开店注册助手”，适配约 2 分钟讲解；PPT 里必须放当前网页截图，最终交付到 `/Users/bianyawen/Desktop/黑客松/跨境开店注册助手-两分钟介绍.pptx`。

### 本轮处理

- 使用 Presentations skill 和 product-platform profile 制作项目介绍 PPT。
- 按计划组织 5 页内容：
  - 项目一句话。
  - 痛点与用户。
  - 产品闭环。
  - 当前网页 Demo。
  - 技术与价值。
- 通过 `playwright screenshot` 重新截取 `http://127.0.0.1:3000/workspace` 当前页面，没有复用旧截图。
- 用 artifact-tool 生成可编辑 PPTX、预览图、contact sheet 和布局 JSON。
- 修复两处布局检查发现的文字框挤压风险，重新导出最终 PPTX。
- 最终文件已复制到项目根目录：`跨境开店注册助手-两分钟介绍.pptx`。

### 验证证据

- `git log --oneline -3`：失败，当前目录仍不是 Git 仓库。
- `curl -I http://127.0.0.1:3000/workspace`：HTTP 200。
- `playwright screenshot --viewport-size 1440,900`：成功生成当前网页截图。
- artifact-tool 构建：成功导出 5 页 PPTX，文件大小约 166856 bytes。
- PPTX 包检查：`slides: 5`、`media: 1`、`emptyMedia: []`。
- layout quality：0 error，1 accepted warning；warning 是第 3 页护栏 band 标题和正文手动分列。
- contact sheet 已人工查看，5 页均可读，截图清晰。

### 遗留说明

- `npm run build` 本轮被已有 Next build 锁阻塞，返回 `Another next build process is already running`，未强行清锁。
- PPT 制作不涉及业务代码修复；前序业务准确性问题仍未处理。


## 2026-05-18 09:47 CST — 基于 React Bits 制作 `/workspace` 交互 UI

### 用户原始需求

用户要求访问 [DavidHDev/react-bits](https://github.com/DavidHDev/react-bits)，并基于该项目制作 `/workspace` 的交互 UI，同时指定使用 Chrome 和 in-app browser。

### 本轮处理

- 使用 Chrome 打开 `https://github.com/DavidHDev/react-bits`，确认仓库是 animated / interactive / customizable React components 集合。
- 本地只读克隆 React Bits，查看 `SpotlightCard`、`Magnet`、`TiltedCard`、`AnimatedContent`、`Folder` 等组件的实现方向。
- 没有直接安装 React Bits，也没有新增 `motion/react`、`gsap` 等依赖；改为在当前项目中实现轻量交互层。
- 在 `src/app/workspace/page.tsx` 新增：
  - `InteractiveSurface`：跟随鼠标的 spotlight 和轻微 tilt。
  - `MagnetWrap`：按钮磁性位移。
  - `RevealOnView`：路线图进入视口后逐步显现。
- 在 `src/app/globals.css` 新增对应交互样式，并提供 `prefers-reduced-motion` 降级。
- 将交互应用到当前问题区、深色办理摘要、风险/材料卡、开店档案、官网入口、模板文件抽屉、路线图步骤和材料卡。

### 验证证据

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- in-app browser 刷新 `http://localhost:3000/workspace`：页面存在 8 个 `.interactive-surface`、5 个 `.magnet-wrap`。
- 点击“开始注册引导”后进入“你想注册的经营主体类型是？”。
- DOM 检查：官方入口 `href` 与 `data-official-url` 仍是真实市场监管总局规范链接。
- 页面正文检查无 `DeepSeek`、`Tavily`、`Brave`、`Claude`、`Anthropic`、`自适应主控`、`动态问诊`。
- 控制台没有新增 error/warning。

### 遗留说明

- 本轮是交互层改造，不处理业务准确性问题。
- 没有等待 DeepSeek 生成完整最终方案，因此路线图 `RevealOnView` 的最终视觉效果未在真实最终方案场景中截图验收；代码、构建和首问路径已验证。

## 2026-05-18 08:46 CST — `/workspace` 暖色文档办理工作台 UI 重构

### 用户原始需求

用户要求按计划把 `/workspace` 从 Shopify 薄荷绿任务台改为 Claude-inspired 的“暖色文档办理工作台”：奶油画布、珊瑚色主操作、深色产品面板、编辑感标题、少阴影、用色块和排版建立层级；保留现有问答、官网入口、模板下载、最终方案逻辑，只重构视觉、布局和展示优先级；可见 UI 不出现 DeepSeek、Tavily、Brave、Claude、Anthropic、自适应主控、动态问诊等词。

### 本轮处理

- 读取项目上下文，确认 `HANDOFF.md` 不存在，当前目录仍不是 Git 仓库。
- 读取 `VoltAgent/awesome-design-md` 中的 Claude 风格设计说明，作为视觉语言参考；只抽取色彩、排版、层级方法，不使用品牌名称、标志或官方资产。
- 更新 `DESIGN.md`，替换 Shopify-inspired 规则为“暖色文档办理工作台”规则。
- 更新 `src/app/globals.css`：暖奶油背景、珊瑚主色、深色摘要面板、cream panel、editorial heading 等样式 token 和组件类。
- 更新 `src/components/ui/button.tsx`：按钮从 black pill/mint hover 改为 8px 圆角珊瑚主按钮、奶油 outline、深色 secondary。
- 更新 `src/app/workspace/page.tsx`：
  - 首屏改为左侧“当前要确认”问题区 + 右侧深色“办理摘要”面板。
  - 当前问题、风险、材料、最重要官网入口前置。
  - 官方入口、待补材料、已确认信息保留为折叠/紧凑信息区。
  - 最终方案顺序调整为路线摘要、文件抽屉、办理路线图、材料分组。
  - 保留真实 `href`、`data-official-url`、`data-template-id` 和模板下载逻辑。

### 验证证据

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- in-app browser 打开 `http://localhost:3000/workspace`：清空会话后首屏显示暖奶油背景、珊瑚按钮、深色办理摘要面板。
- 点击“开始注册引导”后进入“你想注册的经营主体类型是？”，官方入口仍出现。
- DOM 检查：官方入口 `href` 和 `data-official-url` 都是真实市场监管总局规范链接。
- 页面正文检查无 `DeepSeek`、`Tavily`、`Brave`、`Claude`、`Anthropic`、`自适应主控`、`动态问诊`、`航线`、`舱单`、`关卡`、`靠港`、`商家启动台`。

### 遗留说明

- Base UI Progress 内部会插入一个视觉隐藏的 `x` 用于进度条可访问性，已为 `/workspace` 进度条补充中文 `aria-label` 和 `getAriaValueText`；该字符不可见，但自动化读取 `body.innerText` 时仍可能出现。
- 当前目录仍不是 Git 仓库。
- 本轮只改 UI、视觉、布局和文案展示，不修业务准确性问题。

## 2026-05-18 00:10 CST — 重新打开 `/workspace` 网页运行测试

### 用户原始需求

用户要求：“打开网页运行测试一下”。

### 本轮处理

- 按项目规则读取 `AGENTS.md`、`MEMORY.md`、`对话.md`、`开发日志.md`、`README.md`，确认 `HANDOFF.md` 不存在。
- 确认当前目录仍不是 Git 仓库，`git log --oneline -3` 返回 `fatal: not a git repository`。
- 初次访问 `http://127.0.0.1:3000/workspace` 失败，因为 3000 端口没有服务监听。
- 启动开发服务器：`npm run dev -- --hostname 127.0.0.1 --port 3000`，Next dev server 成功启动。
- 用 in-app browser 打开 `/workspace`，页面可见。
- 页面恢复了旧最终方案，因此先点击 `重新开始` 清空会话，再点击 `开始注册引导` 从头测试。
- 真实跑通一条上海公司路径：
  - 有限责任公司。
  - 上海。
  - 当前页面实际返回的跨境电商选项。
  - 普通有限责任公司。
  - 注册资本、名称、经营范围、许可项目、住宅地址等动态追问。
  - 本轮出现经营范围调整分支，选择调整为跨境电商相关。
  - 许可项目选择不涉及。
  - 住宅地址填写测试地址。
  - 生成完整办理方案。

### 验证证据

- 首屏、重新开始、开始注册引导、单选、文本输入、多选/动态分支、生成完整办理方案均可用。
- 上海官方入口正常出现，包括上海企业登记在线、名称自主申报告知页、官方教程/依据等。
- 最终方案生成成功，`workflow-final-output` 文本长度约 20254。
- 最终方案包含办理路线图、第 1 步、官网 URL、查看官方教程/依据、身份证复印件/扫描件、住改商材料、跨境电商/Amazon/进出口相关内容。
- 按本轮测试时间过滤，浏览器控制台没有新增 error/warning。
- 页面没有出现 `TAVILY`、`Brave` 或 `搜索 API Key` 报错。
- 已保存截图：`workspace-browser-retest-20260517.png`。

### 观察

- DeepSeek 每轮返回的按钮文案会动态变化，测试时不能固定依赖旧文案，比如“跨境电商”选项描述和名称确认按钮本轮都与上轮不同。
- 最终方案生成仍需要约 58 秒，演示时应保留明确加载提示。

### 结论

网页已打开并完成真实流程测试，可以继续让用户在浏览器中手动测试；本轮没有发现新的页面崩溃或控制台报错。

## 2026-05-17 23:49 CST — `/workspace` 真实浏览器功能测试

### 用户原始需求

用户要求：“运行测试一下这个网页”。上下文是 `/workspace` 页面已经修过按钮交互并配置过新的 Brave Search Key，用户希望直接测试当前网页功能是否能正常使用。

### 本轮处理

- 按项目规则读取 `AGENTS.md`、`MEMORY.md`、`对话.md`、`开发日志.md`、`README.md`，确认 `HANDOFF.md` 不存在。
- 再次确认当前目录不是 Git 仓库，`git log --oneline -3` 返回 `fatal: not a git repository`。
- 确认 3000 端口有 Next dev server 监听，`curl -I http://127.0.0.1:3000/workspace` 返回 HTTP 200。
- 使用 in-app browser 打开 `http://127.0.0.1:3000/workspace`，先点 `重新开始` 清空旧会话，再从头测试主流程：
  - `开始注册引导`。
  - 选择 `有限责任公司`。
  - 输入 `上海`。
  - 选择 `跨境电商`。
  - 选择普通 `有限责任公司`、个人股东。
  - 输入注册资本 `50万`。
  - 输入字号和行业表述 `海跃，跨境电子商务`。
  - 确认名称和备选名称。
  - 多选经营范围：货物进出口、技术进出口、互联网销售、电子商务。
  - 选择 `住宅地址（需住改商）`。
  - 点击 `生成完整办理方案`。
- 测试最终方案 DOM，确认 `workflow-final-output` 真实生成了 2 万多字符的方案，不是只显示顶部介绍中的“办理路线图”。
- 额外跑了 4 个 `/api/ai/workflow-final-plan` 场景矩阵：上海普通跨境公司、上海一人软件公司、上海个体户服装电商、成都普通餐饮公司。

### 验证证据

- 浏览器主流程能从首屏进入首问，单选、多选、文本提交、最终方案按钮均可用。
- 上海路径能显示真实官方入口链接，例如上海企业登记在线、名称自主申报告知页、上海企业登记在线开办指南 PDF、上海一网通办、市场监管总局 2026 材料规范。
- 最终方案包含办理路线图、第 1 步名称自主申报、第 2 步登记材料准备、身份证复印件/扫描件、公司章程、住改商/利害关系人同意材料、跨境电商附加事项、Amazon/进出口相关内容。
- 按本次测试开始时间过滤后，浏览器控制台没有新增 error/warning；之前 `shouldShowSubmitButton is not defined` 和 React script 警告是旧热更新日志。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，仅有既有 Node `module.register()` 弃用警告。

### 发现的问题

- 普通多人有限公司仍可能混入一人有限公司风险文本。上海普通多人跨境公司和成都普通餐饮公司矩阵结果都出现了 `hasOnePersonRisk:true`。
- 上海个体户方案主路线不含公司章程，但文本里仍可能出现一人有限公司/银行对公账户/公司化事项，说明主体类型过滤还不够彻底。
- 成都等非上海地区仍主要是通用政务入口卡，缺少上海这种高细节官方教程和点击路径。
- 最终方案接口耗时仍偏长，4 个矩阵场景约 52-59 秒；演示时用户容易误以为卡住。

### 结论

网页功能链路可跑通，当前可以让用户继续手动测试；下一轮建议优先修 `final-plan.ts` 的风险过滤和主体类型文本过滤，再逐步补成都、深圳、北京、厦门等地区官方资料包。

## 2026-05-17 23:32 CST — 更新 Brave Search API Key

### 用户原始需求

用户提供新的 Brave Search API Key，说明旧 key 额度用完，要求把新 key 填进去。

### 本轮处理

- 更新 `.env.local`，写入 `BRAVE_API_KEY` 和 `BRAVE_SEARCH_API_KEY` 两个变量名，以兼容之前支持过的 Brave Search 配置方式。
- 没有在终端输出、文档记录或最终回复中展示密钥明文。
- 重启 3000 端口的 Next dev server，让 `.env.local` 新变量生效。

### 验证证据

- `.env.local` 当前变量名检查：`DEEPSEEK_API_KEY`、`BRAVE_API_KEY`、`BRAVE_SEARCH_API_KEY` 均存在。
- 使用 `DOTENV_CONFIG_PATH=.env.local node -r dotenv/config` 验证：三个变量均为 set，只输出长度，不输出密钥。
- `.gitignore` 已包含 `.env*`、`.env.local`，不会把本地密钥提交。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：3000 端口由重启后的 node 进程监听。

### 说明

当前代码中的地区上下文仍走内置官方入口和 DeepSeek，不主动调用 Brave Search；本次只是先把 Brave Key 配好，后续如果恢复或新增 Brave 搜索功能，可以直接读取这些变量。

## 2026-05-17 23:21 CST — 修复 `/workspace` 按钮点击无响应

### 用户原始需求

用户反馈：“点击按钮没有反应”，随后要求“继续”。上下文是 `/workspace` 页面在 in-app browser 中测试，用户希望点击开始、选项和办理方案按钮都能真实推进流程，而不是卡住或暴露内部技术词。

### 本轮处理

- 修复半成品兜底脚本导致的 TypeScript 编译错误：原脚本在 `String.raw` 中嵌套模板字符串，导致 `page.tsx` 无法通过编译。
- 将兜底脚本的 HTML 拼接改为普通字符串拼接，避免再次破坏 TSX 解析。
- 将兜底点击逻辑改为延迟备用：
  - 正常情况下先让 React 的 `onClick` 处理。
  - 如果嵌入式浏览器事件挂载异常、页面没有推进，再由兜底逻辑调用 `/api/ai/workflow-step` 或 `/api/ai/workflow-final-plan`。
- 改为使用 Next `Script strategy="afterInteractive"` 注入兜底脚本，避免 React 客户端渲染 `<script>` 的警告。
- 去掉兜底脚本对 `<html>` 的预水合属性写入，避免 hydration mismatch。
- 在 `next.config.ts` 加入 `allowedDevOrigins: ['127.0.0.1']`，解决用户当前使用 `127.0.0.1:3000` 时 Next dev HMR 被跨源拦截的问题。
- 调整 UI 交互：
  - 单选题下方不再显示“提交并继续”，避免误点无效按钮。
  - 只有文本题和多选题显示“提交并继续”。
  - 最终方案按钮只有信息足够时才启用。
  - 首问和说明去掉 `AI/DeepSeek/字段/资料包` 等内部表达。

### 验证证据

- `git log --oneline -3`：仍失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有 Node `module.register()` 弃用警告。
- in-app browser 新页验证：
  - 点击“重新开始”后能回到首屏。
  - 点击“开始注册引导”能进入“你想注册的经营主体类型是？”。
  - 首问单选题不再出现底部“提交并继续”。
  - “生成完整办理方案”在信息不足时保持禁用。
  - 点击“有限责任公司”能推进到地区问题。
  - 点击“上海”能出现上海官方入口卡，包括上海有限公司设立登记、上海企业登记在线等。
  - 按本次测试开始时间过滤后，浏览器无新增 error/warning。
- 当前 dev server 仍在 3000 端口监听，可直接访问 `http://127.0.0.1:3000/workspace`。

### 遗留问题

- 当前目录仍不是 Git 仓库。
- 本轮只修复交互、构建和可见文案残留，没有处理上一轮 QA 中发现的业务准确性问题：普通公司误触发一人公司风险、个体户文本混入公司事项、行业英文值映射错误。
- DeepSeek 动态生成的解释文字偶尔仍偏“系统说明”，后续可继续收敛 prompt 或在 UI 中只展示更短的用户向解释。

## 2026-05-17 22:37 CST — `/workspace` 产品化文案调整

### 用户原始需求

用户指出当前页面不像产品，文案里有很多内部词和技术词，例如“DeepSeek 自适应主控”“DeepSeek 地区化”“AI 营业执照注册动态引导”“AI 动态问诊”等；用户认为这些没有必要显露出来，应该换成产品名字和更准确的引导话术。

### 本轮处理

- 将 `/workspace` 首屏标题从“AI 营业执照注册动态引导”改为“跨境开店注册助手”。
- 将顶部标签从技术实现表达改为产品能力表达：
  - 一步步引导。
  - 地区入口匹配。
  - 材料与风险核验。
- 将主卡片从“当前 AI 问题”改为“当前要确认的信息”。
- 将首屏启动区从“第一步：DeepSeek 自适应问诊 / 开始 AI 动态问诊”改为“第一步：确认注册方向 / 开始注册引导”。
- 将最终方案区改为“办理方案 / 生成完整办理方案”。
- 将右侧信息区从“会话状态 / 问答历史”改为“当前资料 / 已确认信息”。
- 增加 `toUserFacingMessage()`，把错误、通知和说明里的模型名、资料包、护栏等内部词替换为用户可理解的表达。
- 导出标题从“DeepSeek 自适应注册方案”改为“注册办理方案”，导出副标题改为产品名。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。
- in-app browser 刷新 `http://127.0.0.1:3000/workspace` 后，首屏显示“跨境开店注册助手”和“开始注册引导”。
- 浏览器正文检查：不再包含 `DeepSeek`、`Tavily`、`Brave`、旧标题“AI 营业执照注册动态引导”。

### 遗留问题

- 本轮只处理产品化文案，没有修复上一轮 QA 发现的内容准确性问题。
- 下一步仍建议修复：普通公司误触发一人有限公司风险、个体户混入对公账户/单位社保、行业选项值映射错误。

## 2026-05-17 22:29 CST — `/workspace` 多地区多类目功能 QA

### 用户原始需求

用户要求：“你去测试一下这个网站的功能，就是你输入很多不同的想注册的地点啊，类目这些去完整测试一下，然后分析问题，看有没有做的不足的地方”，并指定使用 in-app browser。

### 本轮处理

- 按项目规则读取 `AGENTS.md`、`MEMORY.md`、`对话.md`、`开发日志.md`、`README.md` 和 `docs/AI_WORKFLOW_DECISION_TREE.md`；确认 `HANDOFF.md` 不存在。
- 确认当前目录不是 Git 仓库，`git log --oneline -3` 失败。
- 确认 `npm run dev` 已在 3000 端口运行。
- 使用 in-app browser 打开 `http://127.0.0.1:3000/workspace`：
  - 首屏能显示完整工作台、免责声明、开始按钮。
  - 控制台没有 error/warning。
  - 自动化点击“开始 AI 动态问诊”未触发页面状态变化；脚本资源已加载，需要用户手动点击确认是否只是自动化环境限制。
- 使用 `/api/ai/workflow-step` 验证问诊链：
  - 初始问题是“你想注册的经营主体类型是？”。
  - 选择公司后询问注册城市。
  - 输入上海后出现上海官方入口卡。
  - 选择跨境电商后，下一问合理地转向股东类型，但 session 把 `cross_border_ecommerce` 当作行业文本，导致行业分类误变成“咨询/服务”。
- 使用 `/api/ai/workflow-final-plan` 跑 6 个场景矩阵：
  - 上海 + 一人有限公司 + 跨境电商 + 住宅地址 + 注册资本 120 万。
  - 上海 + 个体户 + 服装电商 + 租赁地址。
  - 成都 + 普通多人有限公司 + 餐饮 + 租赁地址。
  - 深圳 + 普通多人有限公司 + 软件科技 + 园区地址。
  - 北京 + 个体户 + 自媒体 + 住宅地址。
  - 厦门 + 普通多人有限公司 + 跨境贸易/Amazon + 租赁地址。

### 验证结果

- 6 个最终方案接口都返回 `success:true`。
- 路线图数量为 9-11 步，且每个路线图步骤都有 `officialUrl`。
- 上海一人跨境公司路径完整，能展示名称申报、材料准备、设立登记、地址核验、签名审核、领照、刻章、银行、税务、社保、跨境电商附加事项。
- 上海一人跨境公司能触发注册资本 100 万以上、一人有限公司、住宅地址、跨境电商等风险。
- 上海个体户主路线不含公司章程、股东出资和银行对公账户预约。
- 详细矩阵结果已保存到 `workspace-qa-results.json`。

### 发现的问题

- 普通多人有限公司仍会误出现一人有限公司风险：成都、深圳、厦门普通公司均出现“一人有限公司财产独立/混同”类风险。
- 个体户方案的补充说明仍会混入公司化事项：上海个体户和北京个体户出现“银行开户/银行对公账户/单位社保”等文本。
- 下一问选项值缺少中文语义映射：`cross_border_ecommerce` 写入 session 后无法被 `inferIndustryCategory()` 识别为“跨境电商”，经营范围会错落到“咨询/服务”。
- 非上海地区仍是通用入口卡，深度不够；北京、成都、深圳、厦门没有像上海那样的精确事项卡和教程路径。
- 最终方案生成耗时约 37-62 秒，演示时需要更强的加载反馈或缓存。
- in-app browser 自动化点击未触发状态变化，但控制台无错，需用户手动确认是否为真实交互问题。

### 遗留问题

- 本轮是 QA 复测，没有改业务代码，因此未运行 `npm run lint` / `npm run build`。
- 建议下一轮先修 `final-plan.ts` 的风险和文本过滤，再修 `adaptive-orchestrator.ts` 的选项值映射。

## 2026-05-17 22:01 CST — 修复 `/workspace` 页面报错和恢复态卡死

### 用户原始需求

用户在 in-app browser 打开 `http://localhost:3000/workspace` 后反馈：“这个网页有报错，你去修好后，我去测试”，随后要求“继续”。

### 本轮处理

- 按项目规则读取 `AGENTS.md`、`MEMORY.md`、`对话.md`、`开发日志.md`、`README.md`，确认 `HANDOFF.md` 不存在。
- 确认当前目录仍不是 Git 仓库，`git log --oneline -3` 返回 `fatal: not a git repository`。
- 发现 3000 端口一度没有服务监听，重新启动 `npm run dev`，Next dev server 现在监听 `http://localhost:3000`。
- 修复 `/workspace` 首屏恢复策略：
  - 服务端不再只输出“正在恢复自适应工作流...”。
  - 服务端首屏直接输出默认工作台。
  - 客户端挂载后再异步读取 localStorage 恢复旧会话。
- 修复旧缓存兼容：
  - 归一化 `session/currentQuestion/history/stepResult/finalPlan`。
  - 旧 `finalPlan` 缺 `roadmapSteps/materialsByStep` 时补空数组。
  - 旧 `session` 缺 `location/address` 时补默认字段。
  - 旧 `stepResult/actionCards/materials` 缺数组字段时补空数组。
- 修复 UI 防御渲染：
  - 最终方案、路线图、材料分组、入口卡片、导出内容不再假设数组字段一定存在。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仅有 Node `module.register()` 弃用警告。
- `curl -I http://localhost:3000/workspace`：HTTP 200。
- Playwright 干净 localStorage 验证：页面显示“AI 营业执照注册动态引导”“开始 AI 动态问诊”，无页面崩溃。
- Playwright 注入旧缓存验证：旧 `finalPlan` 缺 `roadmapSteps/materialsByStep` 后刷新仍能显示旧方案，不报 `length/map` 运行时错误。
- in-app browser 验证：访问 `http://127.0.0.1:3000/workspace` 能显示完整工作台，不再卡在“正在恢复自适应工作流...”，`tab.dev.logs` 无 error/warning。

### 遗留问题

- 当前目录仍不是 Git 仓库。
- 如果用户浏览器里还有旧会话，可直接测试；如想从零开始，可点“重新开始”。

## 2026-05-17 21:23 CST — 官方流程顺序、材料细目与官网直连改造

### 用户原始需求

用户要求实现“官方流程顺序、材料细目与官网直连改造计划”：

- `/workspace` 不能只给入口卡片和 AI 文字方案，要按办理顺序告诉用户先办什么、后办什么、最后办什么。
- 每一步要列办理机构、官网入口、官方教程/依据、必备材料、场景材料和注意事项。
- 材料要详细到身份证复印件、地址证明、公司章程、住所证明等；但“公司财产独立证明/财产证明”不能误写成设立必交材料。
- “打开入口”必须是真实 `href`，同时显示可复制 URL。
- 继续只用 DeepSeek，不恢复 Tavily/Brave 搜索；只接已确认官方页面/PDF。

### 本轮处理

- 新增最终方案路线图模型：
  - `WorkflowRoadmapStep`
  - `WorkflowMaterialItem`
  - `WorkflowMaterialsByStep`
  - `WorkflowMaterialCategory`
  - `OfficialSourceKind`
- `FinalWorkflowPlan` 增加 `roadmapSteps` 和 `materialsByStep`，最终方案 UI 优先展示“办理路线图”。
- 上海本地护栏生成固定办理顺序：
  - 名称自主申报。
  - 登记材料准备。
  - 有限公司设立登记 / 个体工商户设立登记。
  - 住所/经营场所核验。
  - 实名认证/电子签名与提交审核。
  - 审核补正与领取营业执照。
  - 公安备案刻章。
  - 公司路径增加银行对公账户预约。
  - 税务登记/发票和申报设置。
  - 社保/公积金开户。
  - 跨境电商场景增加跨境电商附加事项。
- 材料清单按步骤分组：
  - 必备登记材料。
  - 地址相关材料。
  - 人员/签名材料。
  - 后续办理材料。
  - 跨境电商材料。
  - 风险留存材料。
- 一人有限公司的“公司财产独立留存材料”只作为 `risk_retention` 展示，且 `required:false`，明确不是设立登记必交。
- `final-plan.ts` 增加护栏：
  - DeepSeek 可以补充相似步骤内容，但不能删除、重排或插入不适用路线图步骤。
  - 只能使用本地资料包/官方来源白名单中的 URL。
  - 个体户方案过滤公司设立和银行对公开户 actionCards，避免混入公司章程、股东出资等材料。
- `/workspace` 改造：
  - 最终方案顶部展示“办理路线图”。
  - 每步展示“第 N 步”、阶段、标题、办理机构、什么时候办、打开官网、官方教程/依据、可见 URL、具体怎么做、不能继续的情况、材料细目和下一步。
  - “材料缺口”按步骤分组。
  - 官方办理入口卡片显示入口 URL 和依据 URL。
- 稳定性修复：
  - `ExportActions` 改为按需动态加载导出模块，减少首屏依赖。
  - `/workspace` 恢复挂载后读取 localStorage，并改用 `setTimeout`，修复 hydration mismatch。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仍有 Node `module.register()` 弃用警告，但构建成功。
- `/api/ai/workflow-step`：上海公司住宅地址回答后返回 `success:true`，有上海地址核验 actionCards，触发住宅地址风险，无 `TAVILY_API_KEY`/Brave/搜索 Key 文案。
- `/api/ai/workflow-final-plan` 公司场景：上海浦东 + 一人有限公司 + 注册资本 120 万 + 住宅地址 + 跨境电商返回 11 步，包含银行对公账户、跨境电商附加事项、风险留存材料、住改商提醒，并触发注册资本/一人公司/住宅地址风险。
- `/api/ai/workflow-final-plan` 个体户场景：上海黄浦 + 电商网店 + 租赁地址返回 9 步，包含“个体工商户设立登记”，不包含公司章程、股东出资或公司银行对公账户步骤。
- Playwright 普通 Chromium 验收 `/workspace`：页面显示“办理路线图”“第 1 步”“打开官网”“官网 URL”“公司财产独立留存材料”“住改商/利害关系人同意材料”“跨境电商附加事项”；干净控制台无 hydration 错误。

### 遗留问题

- 上海已做成高细节样板；其他地区仍需要逐个补官方教程和事项卡。
- 当前目录仍不是 Git 仓库，`git log --oneline -3` 失败。

## 2026-05-17 20:18 CST — 细化官方办理入口和可执行操作指引

### 用户原始需求

用户要求实现“细化链接与可执行操作指引”计划：

- 把“地区依据来源”和“最终方案”从简单链接列表升级为可操作的办事导航。
- 每个事项显示官方入口、适用场景、要点、点击路径、需要准备的信息、注意事项。
- 运行时继续只用 DeepSeek，不接 Tavily/Brave。
- 上海首批种子来源包括上海企业登记在线、名称自主申报告知页、办理范围说明、上海发改委名称验证说明、上海企业登记在线使用指南 PDF。
- 对“刻章、税务、银行开户”等后续事项，没有确认入口时只写事项级指引和官方核验提醒，不编造具体按钮。

### 本轮处理

- 新增 `RegionalActionSource` / `RegionalActionCategory` 类型，并让下一问、最终方案、地区上下文都支持 `actionCards`。
- 在 `regional-context.ts` 内置上海办理事项资料包：
  - 名称自主申报。
  - 有限公司设立登记。
  - 个体工商户登记。
  - 住所登记与地址材料核验。
  - 名称验证辅助核验。
  - 刻章备案提醒。
  - 税务登记/新办纳税人事项。
  - 对公银行账户预约。
  - 社保/公积金开户事项。
  - 跨境电商附加准备。
  - 2026 全国登记材料规范核验。
- 改造 DeepSeek Prompt：
  - 下一问和最终方案都要求输出 `actionCards`。
  - 不允许只写“去官网办理”。
  - 按钮名称不确定时必须写“在页面搜索/选择对应事项”。
  - 不允许伪造费用、时限和未确认按钮。
- 改造 `/workspace`：
  - “地区依据来源”改名为“官方办理入口”。
  - 卡片展示入口按钮、适用对象、点击路径、准备信息、注意事项和依据页。
  - 最终方案分段也展示相关办理入口卡片。
  - 导出 payload 增加官方办理入口内容。
  - 文本输入框补充 `id/htmlFor`，修复浏览器自动化和可访问性问题。
- 稳定性处理：
  - DeepSeek JSON 工具支持 `maxTokens`。
  - 最终方案生成提高到 8000 tokens。
  - DeepSeek 返回 JSON 格式不稳定时，仅用本地护栏方案兜底；缺 Key、网络失败、模型错误不伪装成功。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仍有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- `/api/ai/workflow-step` 场景验证：上海 + 公司 + 跨境电商返回 `success:true`，包含 10 张 actionCards，前几张为“上海有限公司设立登记”“上海名称自主申报”“上海住所登记与地址材料核验”。
- `/api/ai/workflow-final-plan` 场景验证：上海浦东 + 一人有限公司 + 注册资本 120 万 + 住宅地址 + 跨境电商返回 `success:true`，包含 10 张 actionCards，并触发注册资本、一人有限公司、住宅地址风险。
- 浏览器验证 `/workspace`：填“上海”后页面出现“上海有限公司设立登记”“上海名称自主申报”“打开入口”，且没有 `TAVILY_API_KEY` 提示。

### 遗留问题

- 当前只做了上海高细节样板；其他地区仍是通用政务入口卡片。
- 不做实时搜索，地区资料由 DeepSeek + 本地官方入口资料包组织。
- 当前目录仍不是 Git 仓库。

## 2026-05-17 18:05 CST — 改为只用 DeepSeek 不依赖搜索 Key

### 用户原始需求

用户在 `/workspace` 页面仍看到“缺少搜索 API Key”的错误，明确要求：

- 不要使用 Tavily 或 Brave 搜索工具。
- 只用 DeepSeek。
- 让 DeepSeek 自己生成地区化下一问和内容。

### 本轮处理

- 修改 `src/lib/workflow/regional-context.ts`：
  - `retrieveRegionalContext()` 不再调用任何搜索 API。
  - 直接返回内置官方入口提示和市场监管总局 2026 规范提示。
  - 删除 Tavily/Brave 搜索实现，避免再次误触搜索 Key 依赖。
- 修改 `adaptive-orchestrator.ts` 和 `final-plan.ts`：
  - Prompt 改为要求 DeepSeek 基于自身知识和内置官方入口提示进行地区化判断。
  - 保留“具体办理入口、费用、时限以官方平台最新页面为准”的提示。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `curl /api/ai/workflow-step` 带回答进入下一步：返回 `success:true`，DeepSeek 成功生成下一问，不再报缺少搜索 API Key。

### 遗留问题

- 当前不再真实联网检索，地区化准确性依赖 DeepSeek 模型知识和内置官方入口提示。
- 方案必须继续提示用户以当地市场监管局和政务服务网最新页面为准。

## 2026-05-17 17:50 CST — 修复搜索 Key 只认 Tavily 的问题

### 用户原始需求

用户在 in-app browser 的 `/workspace` 页面看到错误提示缺少 `TAVILY_API_KEY`，指出可以提供 Brave API Key，也已经提供 DeepSeek API Key，要求修复不要卡住。

### 本轮处理

- 检查 `.env.local`，确认当前只检测到 `DEEPSEEK_API_KEY`，未检测到搜索 Key。
- 修改 `src/lib/workflow/regional-context.ts`：
  - 搜索 Provider 从只支持 Tavily 改为自动选择。
  - 优先使用 `TAVILY_API_KEY`。
  - 没有 Tavily 时使用 Brave Search，支持 `BRAVE_API_KEY`、`BRAVE_SEARCH_API_KEY`、`BRAVE_SEARCH_KEY`。
  - Brave 请求使用 `https://api.search.brave.com/res/v1/web/search` 和 `X-Subscription-Token`。
- 移除 `/api/ai/workflow-step` 和 `/api/ai/workflow-final-plan` 中硬编码的 Tavily Key 前置检查，让检索层统一判断。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `curl /api/ai/workflow-step` 带回答时，错误文案已改为缺少搜索 API Key，可配置 Tavily 或 Brave，不再只提示 Tavily。

### 遗留问题

- 需要把 Brave Search Key 写入 `.env.local`，变量名用 `BRAVE_API_KEY` 或 `BRAVE_SEARCH_API_KEY`，然后重启 Next dev server 才能真实验证联网路径。

## 2026-05-17 17:30 CST — DeepSeek 自适应工作流重构

### 用户原始需求

用户要求立即实现计划，但随后明确修正方向：

- 不要把 `docs/AI_WORKFLOW_DECISION_TREE.md` 写成固定流程。
- 要接入 DeepSeek，让 DeepSeek 根据用户回答实时决定下一问和方案内容。
- 固定工作流只作为护栏和基础框架，具体问题、地区化注册流程、材料清单都要动态变化。
- 用户举例：如果用户说“我在上海”，DeepSeek 应接收上海注册流程相关资料，并据此改变后续问题和内容。
- 本次范围是全链路重构，不只是下一问。

### 本轮处理

- 按项目启动规则读取 `AGENTS.md`、`MEMORY.md`、`对话.md`、`开发日志.md`，确认 `HANDOFF.md` 不存在，`git log --oneline -3` 失败因为当前目录不是 Git 仓库。
- 新增 `src/lib/workflow/` 自适应工作流层：
  - `types.ts`：定义 `UserSession`、`WorkflowQuestion`、`WorkflowStepResult`、`FinalWorkflowPlan` 等结构。
  - `guardrails.ts`：阶段 0-9、风险兜底、材料兜底、免责声明、敏感信息脱敏、行业经营范围模板。
  - `regional-context.ts`：实时地区检索，优先官方来源；缺 `TAVILY_API_KEY` 时明确失败。
  - `adaptive-orchestrator.ts`：合并用户回答、组装地区上下文、调用 DeepSeek 生成下一问。
  - `final-plan.ts`：调用 DeepSeek 生成最终材料清单、注册步骤、后续事项和跨境电商附加步骤。
- 新增 API：
  - `/api/ai/workflow-step`
  - `/api/ai/workflow-final-plan`
- 重写 `/workspace`：
  - 展示当前 AI 问题、为什么问、地区来源、风险提示、材料缺口、会话状态、问答历史和最终方案。
  - 支持返回上一步、重新开始、重试当前判断、生成完整方案和导出。
  - 不再使用旧 6 步固定状态机作为主界面。

### 验证证据

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仍有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- `curl /api/ai/workflow-step` 空会话：返回阶段 0 首问“你想注册的经营主体类型是？”。
- `curl /api/ai/workflow-step` 带回答进入下一步：在当前未配置 `TAVILY_API_KEY` 时返回 `success:false`，明确提示实时地区检索不可用，不伪装实时资料。
- `curl /api/ai/workflow-final-plan`：在当前未配置 `TAVILY_API_KEY` 时返回 `success:false`，不生成伪实时最终方案。

### 遗留问题

- 需要配置 `TAVILY_API_KEY` 后，才能真实验证“上海 + 跨境电商 + 公司”路径是否引用上海企业登记在线/一网通办等地区资料。
- 当前目录仍不是 Git 仓库，比赛提交 GitHub 前需要初始化并提交。
- 真实联网质量依赖 DeepSeek Key、Tavily Key、余额和网络。

## 2026-05-17 15:25 CST — 重构为 AI 开店资料代办工作台

### 用户原始需求

用户指出旧 MVP “很表面”，只是告诉用户准备营业执照、身份证等大家都知道的信息，没有满足真实需求。用户真正想要的是：

- AI 不断追问用户：你有什么资料、有没有公司、准备做什么。
- AI 分析缺哪些资料，帮用户生成相关模板，让用户填一下。
- 如果用户还没开公司，工具要告诉他怎么办营业执照，需要准备哪些材料，并生成模板文件。
- 用户办完营业执照后上传到本地，系统记录统一社会信用代码等字段。
- 最后生成 Amazon 平台需要填写的所有信息清单，用户只需要复制粘贴。

### 本轮处理

- 按 `AGENTS.md` 完成启动检查：已读 `AGENTS.md`、`MEMORY.md`、`开发日志.md`、`README.md`；`对话.md` 原本不存在；`HANDOFF.md` 不存在；`git log --oneline -3` 失败，因为当前目录不是 Git 仓库。
- 调用 Search Agent 获取官方依据：Amazon 中国卖家美国站资料要求、Amazon 注册指南、市场监管总局经营主体登记材料规范。
- 调用 Explore Agent 审计现有代码：确认旧项目是线性清单/表单/报告，缺少工作台、资料库、模板生成、上传解析和填表包。
- 调用 Plan Agent 做只读拆解；同时主控按用户已批准计划推进更完整实现。
- 调用 Worker Agent 独立实现导出工具；主控实现工作台核心。

### 实现结果

- 新增 `/workspace` 工作台，覆盖 AI 问答采集、办照信息、证照解析、本地资料库、缺口分析、模板草稿、Amazon 填表包、导出交付物。
- 新增 5 个 AI API：`/api/ai/next-question`、`/api/ai/vision-extract`、`/api/ai/gap-analysis`、`/api/ai/template-draft`、`/api/ai/amazon-packet`。
- 新增工作台领域模型、本地 IndexedDB 保存工具、统一 API 响应工具、视觉 OCR 专用 prompt。
- 首页入口改为“AI 开店资料代办工作台”。
- 导出支持复制文本、Markdown、HTML、DOCX、打印 PDF。

### 验证证据

- `npm run lint`：通过。
- `npm run build`：通过。
- `curl /api/ai/gap-analysis`：成功返回缺口分析。
- `curl /api/ai/template-draft`：成功返回模板草稿。
- `curl /api/ai/vision-extract`：粘贴营业执照文字成功提取统一社会信用代码、企业名称、法定代表人、注册地址和经营范围。
- 浏览器打开 `http://localhost:3000/workspace`：页面渲染正常；手动走通填写资料、解析证照、生成缺口、模板和填表包。

### 遗留问题

- 当前目录仍不是 Git 仓库，比赛提交 GitHub 前需要初始化并提交。
- `.env.local` 中存在真实形态 DeepSeek Key，虽然 `.gitignore` 已忽略 `.env*`，仍建议轮换密钥，避免截图或误提交泄露。
- 真实 GLM-4V 图片 OCR 需要配置 `ZHIPU_API_KEY` 后继续用真实营业执照图片验证。
- 工具所有模板和报告都必须保持“草稿/风险提示”定位，不构成法律、税务意见，也不保证 Amazon 审核通过。

## 2026-05-18  — `/workspace` 产品级 UI 重构与主题纠偏

### 用户原始需求

用户要求执行 `/workspace` 产品级 UI 重构计划：把页面从“技术工作台/卡片堆叠”改为面向用户的产品界面；实施开始先安装并使用 `vercel-labs/agent-skills@web-design-guidelines`；不改现有 DeepSeek 工作流、API 和数据结构，只重构视觉、文案和可用性。

实施过程中用户进一步纠偏：不要使用“政务蓝”等大多数人都在用的视觉最大公约数设计，要结合这个功能的主题元素重新构思，追求新颖创新，而不是追求稳。

### 本轮处理

- 已安装 `vercel-labs/agent-skills@web-design-guidelines`，并读取官方 Web Interface Guidelines 作为 UI 检查依据。
- 放弃“政务蓝”方向，改为“跨境出海注册航线”主题：注册流程是航线，问题是关卡，材料是舱单，官方入口是靠港入口，历史回答是航迹。
- 修改 `src/app/globals.css`：移除手账纸纹/红色边距线，改为浅绿灰航线网格背景；主题色改为墨绿、航标黄、珊瑚红、纸白和深墨色。
- 修改 `src/app/workspace/page.tsx`：重构为顶部航线控制台、左侧“下一道关卡”主任务区、风险雷达/材料舱单、最终路线图、右侧资料舱单和官方靠港入口。
- 官方入口展示新增“复制链接”按钮；入口按钮文案统一为“打开官网 / 查看依据 / 复制链接”。
- 清理可见产品文案，页面不再出现 `DeepSeek`、`Tavily`、`Brave`、`自适应主控`、`动态问诊`、`AI 营业执照`、`政务蓝` 等内部或不合适表达。

### 验证结果

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，仅有既有 Node `module.register()` 弃用警告。
- in-app browser 刷新 `http://127.0.0.1:3000/workspace`：首屏显示“出海注册航线 / 办理关卡 / 材料舱单 / 下一道关卡”，控制台无 warning/error。
- 浏览器冒烟测试：点击“重新开始”再点击“开始注册引导”，成功进入“你想注册的经营主体类型是？”并显示“个体工商户 / 有限责任公司 / 不确定，帮我分析”等选项。

### 遗留问题

- 本轮只处理 UI 与产品语言，不处理前序 QA 发现的普通公司误触发一人公司风险、个体户文本混入公司事项、行业英文值映射等业务准确性问题。
- 当前目录仍不是 Git 仓库，`git log --oneline -3` 仍失败。

## 2026-05-17 16:33 CST — 改成严格联网的 Agent 逐步问答工作台

### 用户原始需求

用户在浏览器中继续测试后反馈：

- 当前 `/workspace` 很杂乱，一次性铺满所有模块，没有一条简洁逻辑链。
- 按钮点击后多次进入 fallback，结果看起来像离线写死数据。
- 现在的问题太少，不会根据用户回答实时变化，感觉很死。
- 用户要求改成“严格联网的 Agent 逐步问答工作台”：联网时不要默认用离线数据；失败就显示失败原因、重试和显式演示入口。
- 用户要求资料库、模板、填表包按流程逐步解锁，不要首屏全部堆出来。

### 本轮处理

- 按项目启动规则重新读取 `AGENTS.md`、`MEMORY.md`、`开发日志.md`、`README.md`、`对话.md`，并确认 `git log --oneline -3` 失败，因为当前目录不是 Git 仓库。
- 审计现有 `/workspace`，确认页面是多模块并列仪表盘，缺少单一下一步主线。
- 修改 AI API fallback 策略：非演示模式下缺 Key 或调用失败返回失败，不自动 mock。
- 重写 `/workspace` 为分阶段 Agent 状态机：
  1. 现状判断。
  2. 资料问诊。
  3. 证照解析。
  4. 缺口与模板。
  5. 模板草稿。
  6. Amazon 填表包。
- 新增 Agent 状态面板：展示判断依据、已答字段、缺失字段、当前阻塞点和问答历史。
- 本地已有资料时先给“继续上次资料包 / 新建资料包”选择，新建会清空旧演示状态。
- 显式演示模式下才允许 mock，并扩展 mock 追问链。

### 验证证据

- `npm run lint`：通过。
- `npm run build`：通过；仍有 Node `module.register()` 弃用警告和 localStorage 实验警告，但构建成功。
- 使用无 Key 环境启动测试服务后验证：
  - `/api/ai/gap-analysis`：返回 `success:false`，不返回 mock 缺口分析。
  - `/api/ai/template-draft`：返回 `success:false`，不返回 mock 模板。
  - `/api/ai/amazon-packet`：返回 `success:false`，不返回 mock 填表包。
  - `/api/ai/next-question`：返回 `success:false`，不返回 mock 下一问。
  - `/api/ai/vision-extract` 图片输入且无 `ZHIPU_API_KEY`：返回失败。
  - `/api/ai/vision-extract` 粘贴营业执照文字：返回 `source:"manual"`，能提取统一社会信用代码等字段。
- 浏览器验证 `http://localhost:3000/workspace`：
  - 本地已有资料时只显示继续/新建选择。
  - 新建资料包后只显示第一个问题，不再堆资料库/模板/填表包。
  - 点击“还没公司/营业执照”后，严格联网失败会显示错误、重试和“开启演示模式并生成”。
  - 显式开启演示模式后才生成 mock 下一问。

### 遗留问题

- 当前目录仍不是 Git 仓库，比赛提交 GitHub 前需要初始化并提交。
- `.env.local` 中曾出现真实形态 DeepSeek Key，建议轮换密钥并避免提交或截图泄露。
- 真实联网质量仍需用有效 `DEEPSEEK_API_KEY` 验证。
- 真实 GLM-4V 图片 OCR 仍需配置 `ZHIPU_API_KEY` 后用实际营业执照图片验证。

## 2026-05-17 16:46 CST — 修复 DeepSeek 已配置但下一问失败

### 用户原始需求

用户看到页面显示：“AI 下一问调用失败，严格联网模式下不自动使用离线追问。请检查 API Key、余额或网络后重试”，并指出已经提供 DeepSeek API Key，要求修复。

### 本轮处理

- 检查 `.env.local`，确认 `DEEPSEEK_API_KEY` 存在；未打印完整密钥。
- 直接请求 DeepSeek 官方兼容接口，确认 `deepseek-chat` 和 `deepseek-reasoner` 都返回 HTTP 200。
- 判断失败根因不是 Key，而是当前 AI SDK `generateObject` 结构化调用与 DeepSeek 响应格式兼容不稳定。
- 新增 DeepSeek 原生 JSON 请求工具。
- 将下一问、缺口分析、模板生成、填表包 4 个文本 AI 路由改为 DeepSeek `/chat/completions` 原生请求。
- 兼容 DeepSeek 下一问 JSON 中 `options` 缺失的情况，默认空数组。
- 补齐前端对 `companyPlan.entityType` 的写入。

### 验证证据

- 直接请求 DeepSeek：HTTP 200。
- `curl /api/ai/next-question`：返回 `success:true`、`fallback:false`。
- 浏览器点击“重试”：页面显示“AI 下一问完成”，并出现真实问题“您计划注册的公司名称是什么？”。
- `npm run lint`：通过。
- `npm run build`：通过。

### 遗留问题

- 后续还需要用真实 Key 验证缺口分析、模板生成和填表包在完整档案上的稳定性。
- `ZHIPU_API_KEY` 仍未配置，真实图片 OCR 还不能验证。

## 2026-05-18 02:03 CST — `/workspace` Shopify-inspired 任务台布局重构

### 用户原始需求

用户要求按 `/workspace` Shopify-Inspired 产品 UI 重构计划实施：基于 VoltAgent/awesome-design-md 的 Shopify Inspired DESIGN.md，采用 cream canvas、aloe/pistachio、black pill button 的商家 onboarding 视觉，不再使用政务蓝或航线主题。随后用户继续指出：布局也要大更改，把重要的往前放，该检验的检阅，有些信息可以收起/展开；当前布局没有突出重点，像在看文章。

### 本轮处理

- 读取项目规则、`MEMORY.md`、`对话.md`、`开发日志.md`、`README.md`，确认当前目录仍不是 Git 仓库。
- 联网读取 Shopify Inspired DESIGN.md，提取 transactional light track 的视觉规则。
- 新增项目本地 `DESIGN.md`，记录本项目采用的 Shopify-inspired 规则、组件原则和禁用项。
- 修改 `src/app/globals.css`：cream canvas、aloe/pistachio accents、black ring、`ss03`、merchant card 类。
- 修改 `src/components/ui/button.tsx`：按钮变为 pill 体系，主按钮黑底白字，次级按钮白底黑边，secondary 使用 aloe。
- 修改 `src/app/workspace/page.tsx`：
  - 顶部压缩为产品名、阶段进度和三项关键计数。
  - 主区首屏直接显示“当前开店决策”和当前问题。
  - 右侧信息从普通卡片改为可展开面板：官方办理入口、待补材料、已确认信息。
  - 免责声明改为可展开“办理前说明”。
  - 最终方案中的材料分组、补充说明、官方入口改为可展开面板。
  - 去掉“出海/航线/关卡/舱单/靠港/航迹”等上一版隐喻。

### 验证证据

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。
- in-app browser 打开 `http://127.0.0.1:3000/workspace`：首屏可见产品名、阶段进度、当前开店决策、主体类型问题和选项，不再先铺长说明。
- 点击 `有限责任公司` 后成功推进到省份问题，并更新待补材料数量。
- 页面正文检查无 `DeepSeek`、`Tavily`、`Brave`、`动态问诊`、`自适应主控`、`AI 营业执照`、`出海`、`航线`、`关卡`、`舱单`、`靠港`、`航迹`。

### 遗留问题

- 本轮只处理 UI、布局和文案；前序 QA 中的业务准确性问题仍未修复：普通公司误触发一人公司风险、个体户混入公司事项、行业英文 value 映射错误。
- 当前目录仍不是 Git 仓库，比赛提交前仍需初始化 Git 并提交。
- 补充产物：保存浏览器验收截图 `workspace-shopify-task-ui-20260518.png`。

## 2026-05-18 06:37 CST — 启动 `/workspace` 网页

### 用户原始需求

用户要求：“启动一下网页”。

### 本轮处理

- 按项目规则读取 `AGENTS.md`、`MEMORY.md`、`开发日志.md`，并确认当前目录不是 Git 仓库。
- 检查 3000 端口，发现没有服务监听。
- 执行 `npm run dev -- --port 3000` 启动当前 `/Users/bianyawen/Desktop/黑客松` 项目的 Next dev server。
- 验证 `/workspace` HTTP 200。
- 用 in-app browser 新开标签访问 `http://localhost:3000/workspace`。

### 验证证据

- Next dev server 显示 `Ready in 1343ms`。
- `curl -I http://127.0.0.1:3000/workspace` 返回 HTTP 200。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P` 显示 node 监听 3000。
- 浏览器页面正文包含“跨境开店注册助手”“当前开店决策”“开始注册引导”。

### 说明

- 发现另一个目录 `/Users/bianyawen/Desktop/黑客一` 的 dev server 正在 3001 端口运行，但本轮启动的是当前 `/Users/bianyawen/Desktop/黑客松` 项目。

## 2026-05-18 07:27 CST — 修复官网跳转与模板文件下载

### 用户原始需求

用户要求按计划实现 `/workspace` 官网跳转与模板下载修复：

- “打开官网”按钮必须真实可打开，不能只有文字或无效跳转。
- 生成办理方案后，用户要能下载公司章程草案、住所证明草案、身份证复印件准备清单、材料清单、Amazon 填表清单等文件。
- 每个模板支持 DOCX、Markdown、HTML，全部模板支持 ZIP 打包下载。
- 不额外调用模型，基于当前最终方案和用户状态本地生成。
- UI 不暴露 DeepSeek、Tavily、Brave、自适应主控、动态问诊等内部词。

### 本轮处理

- 新增模板导出工具，按 `FinalWorkflowPlan + UserSession` 生成场景化模板。
- 公司场景生成公司章程草案；个体户不生成公司章程。
- 一人有限公司生成财产独立留存清单，并明确不是设立登记必交材料。
- 跨境电商场景生成 Amazon 注册填表清单和跨境电商后续事项清单。
- 每个模板支持 DOCX、Markdown、HTML 下载；全部模板用 ZIP 打包。
- 官网入口从普通新标签链接升级为统一入口组件：保留真实 `href`，点击优先新窗口打开，被拦截时复制链接并提供当前页打开。
- 页面最终方案顶部新增“模板文件下载”区；未生成方案前提示生成后可下载模板。
- 清洗 UI 枚举值和风险来源，把 `cross_border_ecommerce` 显示为“跨境电商”，把 `deepseek` 显示为“方案生成”。

### 验证证据

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和构建期 localStorage 实验警告。第一次 build 因 Google Fonts 临时请求失败，重试后成功。
- 代码级模板场景验证：公司 + 一人有限公司 + 跨境电商会生成公司章程、一人公司财产独立留存、Amazon 注册填表清单；个体户不会生成公司章程。
- 浏览器验证：`http://localhost:3000/workspace` 首屏正常；页面显示“生成完整办理方案后可下载公司章程草案、住所证明草案、材料清单和平台填表清单”；选择“有限责任公司”并输入“上海”后，官方入口中“打开官网/查看依据”是带真实 URL 的链接，包含上海企业登记在线、上海指南 PDF、市场监管总局规范 URL。

### 遗留说明

- 当前目录仍不是 Git 仓库。
- in-app browser 自动化环境的 evaluate 不暴露 `localStorage`，不能用本地状态注入直接模拟最终方案下载区；已用代码级模板生成验证和真实页面入口验证覆盖核心功能。
- 完整最终方案生成仍依赖 DeepSeek 响应耗时和质量。
