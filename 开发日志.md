# 开发日志

> 每次开发Session结束后更新此文件，记录进展和文件变更。

---

## Session 24 — Vercel 快速演示部署

**时间**: 2026-05-18 12:12 CST

**用户原始需求**:
- 用户要求实施 Vercel 快速演示部署计划。
- 用户随后要求“继续部署”。

**解决的问题**:
- 初始化 Git 仓库并创建 GitHub 仓库：`https://github.com/byxacb/haikesong-cross-border-guide`。
- 增加 `.vercelignore`，避免上传密钥、本地构建缓存、依赖、自动化日志、PPT 和生成输出。
- 创建 Vercel 项目 `humdeci/haikesong-cross-border-guide`，关闭 SSO 部署保护。
- 将 `DEEPSEEK_API_KEY`、`BRAVE_API_KEY`、`BRAVE_SEARCH_API_KEY` 写入 Vercel Production 环境变量。
- 修正 Vercel 项目 framework 为 `nextjs`。
- 使用 `vercel build --prod` + `vercel deploy --prebuilt --prod` 完成生产部署。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | `.vercelignore` | Vercel 部署忽略规则 |
| 修改 | `.gitignore` | 忽略 Playwright 日志、outputs 和 Qoder repowiki |
| 修改 | `MEMORY.md` | 记录部署结果、验证证据和限制 |
| 修改 | `对话.md` | 记录用户需求、处理过程和结果 |
| 修改 | `开发日志.md` | 记录本次 Session |

**验证步骤**:
- `npm run lint`：通过。
- `npm run build`：通过。
- Vercel inspect：生产部署 `dpl_9F98xLzR8aKTWfMaRR4Pg6EBrDnH` 为 `READY`。
- `curl -I https://haikesong-cross-border-guide.vercel.app/workspace`：HTTP 200。
- 页面 HTML 检查：包含 `跨境开店注册助手`、`开始注册引导`；不含 `DeepSeek`、`Tavily`、`Brave`、`搜索 API Key`、`自适应主控`、`动态问诊`。
- `POST /api/ai/workflow-step`：HTTP 200，`success:true`，返回首问。
- Playwright 生产页面验证：打开 `/workspace`，点击 `开始注册引导` 后进入“你想注册的经营主体类型是？”；控制台 0 error / 0 warning。
- `POST /api/ai/workflow-final-plan` 上海跨境电商公司场景：HTTP 200，`success:true`，返回 11 步路线图、11 组材料、10 个官方入口卡。

**当前进度**: 网站已上线到 Vercel，生产地址为 `https://haikesong-cross-border-guide.vercel.app/workspace`。

**下一步**:
- 用户需要用手机或比赛现场网络打开生产地址，确认大陆网络实际可访问性。
- 若大陆访问不稳定，切换到腾讯云/阿里云大陆区域并准备 ICP 备案或本地演示兜底。
- Vercel GitHub 自动部署仍需在控制台授权 GitHub App 访问新仓库。

---

## Session 23 — 两分钟项目介绍 PPT 制作

**时间**: 2026-05-18 11:09 CST

**用户原始需求**:
- 用户要求执行已确认的 PPT 制作计划，生成 5 页中文 16:9 项目介绍 PPT，适合约 2 分钟讲解。
- PPT 必须包含当前 `/workspace` 网页截图，最终交付到项目根目录。

**解决的问题**:
- 使用 Presentations skill 制作 `product-platform` 类型项目介绍 PPT。
- 设计 5 页结构：项目一句话、痛点与用户、产品闭环、当前网页 Demo、技术与价值。
- 通过 Playwright CLI 重新截取当前 `http://127.0.0.1:3000/workspace` 页面。
- 用 artifact-tool 生成可编辑 PowerPoint、预览图和 contact sheet。
- 根据 layout 检查修复第 4 页标题说明重叠风险和第 5 页文字框 padding 风险。
- 将最终 PPTX 复制到项目根目录。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | `跨境开店注册助手-两分钟介绍.pptx` | 最终可编辑 PowerPoint |
| 新增 | `outputs/manual-20260518-ppt/presentations/项目介绍PPT/` | PPT 生成工作区、截图、预览、QA 记录 |
| 修改 | `MEMORY.md` | 记录本次 PPT 制作、验证证据和限制 |
| 修改 | `对话.md` | 记录用户需求、处理过程和结果 |
| 修改 | `开发日志.md` | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run build`：被已有 Next build 锁阻塞，提示 `Another next build process is already running`。
- `curl -I http://127.0.0.1:3000/workspace`：HTTP 200。
- `playwright screenshot --viewport-size 1440,900 http://127.0.0.1:3000/workspace .../workspace-current.png`：成功。
- artifact-tool 构建 PPTX：成功，5 页，约 166856 bytes。
- PPTX 包检查：`slides: 5`、`media: 1`、`emptyMedia: []`。
- layout quality：0 error，1 accepted warning。
- contact sheet 视觉检查：5 页可读，截图清晰。

**当前进度**: 两分钟项目介绍 PPT 已完成并放在项目根目录。

**下一步**:
- 比赛提交前仍需初始化 Git 仓库并准备 GitHub 链接。
- 若继续打磨演示，可基于这份 PPT 录制 5 分钟以内 Demo 视频。

---

## Session 17 — 重新打开 workspace 网页测试

**时间**: 2026-05-18 00:10 CST

**用户原始需求**:
- 用户要求打开网页并运行测试。

**解决的问题**:
- 发现 3000 端口没有服务监听，重新启动 Next dev server。
- 使用 in-app browser 打开 `http://127.0.0.1:3000/workspace`。
- 清空旧 localStorage 恢复状态，从空会话重新测试。
- 走通上海有限公司 + 跨境电商 + 住宅地址路径。
- 验证 DeepSeek 动态分支：本轮出现经营范围调整和许可项目判断。
- 生成完整办理方案并检查最终方案 DOM。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | workspace-browser-retest-20260517.png | 本轮浏览器测试截图 |
| 修改 | MEMORY.md | 记录本轮重新打开网页测试结果 |
| 修改 | 对话.md | 记录用户需求、测试路径和结论 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `curl -I http://127.0.0.1:3000/workspace`：初始失败，因为服务未启动。
- `npm run dev -- --hostname 127.0.0.1 --port 3000`：启动成功，Next 显示 `Ready in 1289ms`。
- in-app browser 打开 `/workspace`：页面正常显示。
- 点击 `重新开始` 后从空会话开始，`开始注册引导` 能进入主体类型首问。
- 上海公司路径完整跑通，最终方案生成成功，`workflow-final-output` 约 20254 字符。
- 浏览器控制台按本轮测试时间过滤无新增 error/warning。
- 页面无 `TAVILY`、`Brave`、`搜索 API Key` 报错。

**当前进度**: `/workspace` 当前可打开、可交互、可生成完整办理方案。

**下一步**:
- 继续修复内容准确性：普通公司误混一人公司风险、个体户混公司事项。
- 优化最终方案生成耗时和加载反馈。

---

## Session 16 — workspace 真实浏览器功能测试

**时间**: 2026-05-17 23:49 CST

**用户原始需求**:
- 用户要求运行测试当前 `/workspace` 网页功能。

**解决的问题**:
- 确认 dev server 正在 3000 端口运行，`/workspace` 返回 HTTP 200。
- 使用 in-app browser 从空会话完整跑通一条上海公司注册路径：
  - 开始注册引导。
  - 有限责任公司。
  - 上海。
  - 跨境电商。
  - 普通有限责任公司、个人股东、注册资本 50 万。
  - 名称和经营范围确认。
  - 住宅地址。
  - 生成完整办理方案。
- 验证官方入口链接是真实 href，不是只有文字按钮。
- 验证最终方案真实渲染在 `workflow-final-output`，包含办理路线图、材料明细、官网入口、住改商风险和跨境电商附加事项。
- 跑 4 个最终方案接口矩阵场景，检查上海公司、上海一人有限公司、上海个体户、成都餐饮公司。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | workspace-browser-final-qa.png | 本次浏览器最终方案测试截图 |
| 修改 | MEMORY.md | 记录浏览器测试、接口矩阵、验证证据和遗留问题 |
| 修改 | 对话.md | 记录用户需求、测试流程、结论和问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：Next dev server 正在监听 3000 端口。
- `curl -I http://127.0.0.1:3000/workspace`：HTTP 200。
- in-app browser：上海公司路径完整跑通，`生成完整办理方案` 后最终方案文本超过 2 万字符。
- 浏览器控制台：按本次测试时间过滤无新增 error/warning。
- `/api/ai/workflow-final-plan` 矩阵：4 个场景均 `success:true`，耗时约 52-59 秒，路线图 9-11 步。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。

**发现的问题**:
- 普通多人有限公司仍可能混入一人有限公司财产独立风险。
- 个体户方案文本仍可能混入公司化后续事项，例如银行对公账户或一人有限公司相关内容。
- 上海之外地区仍是通用入口卡，缺少高细节官方教程路径。
- 最终方案生成耗时偏长，演示时需要更强加载反馈或缓存。

**当前进度**: `/workspace` 页面主功能可测试、可生成完整方案，但业务内容过滤和非上海地区资料包还需加固。

**下一步**:
- 修复 `final-plan.ts` 的风险过滤：普通多人公司不得出现一人有限公司风险，个体户不得出现公司专属事项。
- 继续补成都、深圳、北京、厦门等地区官方入口和教程路径。
- 优化最终方案生成耗时，考虑缓存、分段生成或更明确的加载状态。

---

## Session 15 — 更新 Brave Search API Key

**时间**: 2026-05-17 23:32 CST

**用户原始需求**:
- 用户说明旧 Brave Search API Key 额度用完，提供新的 key，要求写入项目配置。

**解决的问题**:
- 更新 `.env.local`，新增/覆盖 `BRAVE_API_KEY` 和 `BRAVE_SEARCH_API_KEY`。
- 未在日志、文档和回复中记录密钥明文。
- 重启 Next dev server，让新环境变量生效。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | .env.local | 写入 Brave Search 新 key，文件被 `.gitignore` 忽略 |
| 修改 | MEMORY.md | 记录本次配置变更和验证证据，不记录密钥明文 |
| 修改 | 对话.md | 记录用户需求和处理过程，不记录密钥明文 |
| 修改 | 开发日志.md | 记录本次 Session，不记录密钥明文 |

**验证步骤**:
- `sed -n 's/^\\([^#=][^=]*\\)=.*/\\1=<set>/p' .env.local`：显示 `DEEPSEEK_API_KEY`、`BRAVE_API_KEY`、`BRAVE_SEARCH_API_KEY` 均已设置。
- `DOTENV_CONFIG_PATH=.env.local node -r dotenv/config`：三个变量均为 set，只输出长度。
- `rg "^\\.env|env" .gitignore -n`：确认 `.env*`、`.env.local` 被忽略。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：Next dev server 已重启并监听 3000 端口。

**当前进度**: Brave Search 新 key 已配置到本地环境。

**下一步**:
- 当前代码仍不主动调用 Brave Search，地区上下文继续用内置官方入口；如后续要恢复实时搜索，需要重新接入并验证 Brave Search 调用路径。

---

## Session 14 — 修复 workspace 按钮点击无响应

**时间**: 2026-05-17 23:21 CST

**用户原始需求**:
- 用户反馈 `/workspace` 页面“点击按钮没有反应”，要求继续修复。
- 上下文中用户正在 in-app browser 测试 `http://127.0.0.1:3000/workspace`，希望按钮可以真实推进流程，并且页面不要暴露内部技术词。

**解决的问题**:
- 修复 `src/app/workspace/page.tsx` 中原生兜底脚本的 TSX 语法错误，避免 `npx tsc --noEmit` 报模板字符串解析错误。
- 将兜底脚本改为延迟备用，正常先走 React 事件；只有 React 没推进时才兜底调用工作流接口。
- 兜底状态从写入 `<html data-workflow-fallback-bound>` 改为 `window.__WORKSPACE_NATIVE_FALLBACK_BOUND__`，避免 hydration mismatch。
- 兜底脚本改用 `next/script` 的 `afterInteractive` 策略，避免客户端渲染 `<script>` 警告。
- `next.config.ts` 增加 `allowedDevOrigins: ['127.0.0.1']`，修复 127.0.0.1 访问时 Next dev HMR 跨源拦截提示。
- 单选题移除底部“提交并继续”，避免用户误点无效按钮；文本题/多选题保留提交按钮。
- 最终方案按钮改为只有 `canGenerateFinalPlan` 为 true 时启用。
- 首问和可见说明继续收敛产品化文案，去掉 `AI/DeepSeek/字段/资料包` 等内部表达。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/workspace/page.tsx | 修复兜底脚本、按钮交互、单选提交按钮、最终方案启用条件、可见文案 |
| 修改 | src/lib/workflow/guardrails.ts | 移除首问/兜底说明中的内部技术表达 |
| 修改 | src/lib/workflow/adaptive-orchestrator.ts | 移除初始预览文案中的内部技术表达 |
| 修改 | src/lib/workflow/regional-context.ts | 将“资料包”提示改为用户可理解的办理入口说明 |
| 修改 | src/components/ui/button.tsx | 保持原生 `<button>` 组件，减少第三方按钮事件兼容风险 |
| 修改 | next.config.ts | 允许 `127.0.0.1` 作为 Next dev 来源 |
| 修改 | MEMORY.md | 记录本次修复、验证和遗留问题 |
| 修改 | 对话.md | 记录用户反馈、处理过程、验证和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有 Node `module.register()` 弃用警告。
- in-app browser 新页验证：
  - “重新开始”可清空状态。
  - “开始注册引导”可进入主体类型首问。
  - 首问单选题下方不再出现“提交并继续”。
  - 信息不足时“生成完整办理方案”保持禁用。
  - 点击“有限责任公司”可推进到地区问题。
  - 点击“上海”可刷新上海官方办理入口卡。
  - 按测试开始时间过滤，浏览器没有新增 error/warning。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：Next dev server 正在监听 3000 端口。

**当前进度**: `/workspace` 的主要按钮交互恢复，首问和地区选择可在 in-app browser 中实际推进。

**下一步**:
- 继续修复业务准确性：普通公司误触发一人公司风险、个体户混入公司事项、行业英文 value 映射错误。
- 后续可继续收敛 DeepSeek 返回的解释性话术，让用户界面更像产品而不是系统日志。
- 仍需初始化 Git 仓库并提交，满足比赛 GitHub 链接要求。

---

## Session 13 — workspace 产品化文案调整

**时间**: 2026-05-17 22:37 CST

**用户原始需求**:
- 用户指出页面有很多不精准的内部词，例如 `DeepSeek 自适应主控`、`DeepSeek 地区化`、`AI 营业执照注册动态引导`、`AI 动态问诊`。
- 用户要求重新设置文案，让页面像一个产品，而不是技术实现展示。

**解决的问题**:
- 隐藏用户界面中的模型名和内部机制词。
- 将页面标题统一为产品名“跨境开店注册助手”。
- 将顶部标签、主问题卡片、启动按钮、最终方案区、右侧资料区和导出标题统一改为面向用户的产品话术。
- 新增 `toUserFacingMessage()`，将服务端返回的技术性错误或说明转换成用户能理解的表达。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/workspace/page.tsx | 产品化文案、错误提示清洗、导出标题调整 |
| 修改 | MEMORY.md | 记录本次产品文案决策和验证证据 |
| 修改 | 对话.md | 记录用户反馈、处理过程和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。
- in-app browser 刷新 `http://127.0.0.1:3000/workspace`：首屏显示“跨境开店注册助手”“开始注册引导”。
- 浏览器正文检查：不再包含 `DeepSeek`、`Tavily`、`Brave`、旧标题“AI 营业执照注册动态引导”。

**当前进度**: `/workspace` 首屏和主要流程文案已从技术实现展示改为产品化表达。

**下一步**:
- 修复上一轮 QA 发现的内容准确性问题：一人有限公司风险误触发、个体户混入公司后续事项、行业选项值映射错误。
- 继续补齐非上海地区的官方事项卡和教程路径。

---

## Session 12 — workspace 多地区多类目功能 QA

**时间**: 2026-05-17 22:29 CST

**用户原始需求**:
- 用户要求测试 `/workspace` 网站功能，输入多个不同注册地点和类目完整测试，并分析不足。
- 用户指定使用 in-app browser。

**解决的问题**:
- 确认项目上下文、开发服务器和浏览器状态。
- 使用 in-app browser 验证页面首屏可打开，控制台无 error/warning。
- 使用 `/api/ai/workflow-step` 验证逐步问诊链。
- 使用 `/api/ai/workflow-final-plan` 对 6 个地区/主体/行业场景做矩阵测试。
- 产出当前功能问题清单和优先修复建议。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | workspace-qa-initial.png | `/workspace` 首屏截图证据 |
| 新增 | workspace-qa-results.json | 6 个最终方案场景矩阵结果 |
| 修改 | MEMORY.md | 记录本次 QA 结论、验证证据和问题清单 |
| 修改 | 对话.md | 记录用户需求、测试过程和发现问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：Next dev server 正在监听 3000 端口。
- in-app browser 打开 `http://127.0.0.1:3000/workspace`：首屏正常显示；控制台无 error/warning。
- `/api/ai/workflow-step`：初始问题、公司选择、上海地区、跨境电商回答均返回 `success:true`。
- `/api/ai/workflow-final-plan`：6 个场景均返回 `success:true`；路线图 9-11 步；所有路线图步骤都有 `officialUrl`。

**发现的问题**:
- 普通多人有限公司会误触发一人有限公司财产独立/混同风险。
- 个体户补充说明会混入银行对公账户、单位社保等公司化事项。
- `cross_border_ecommerce` 等英文选项值未映射为中文行业，导致经营范围模板可能误判为“咨询/服务”。
- 非上海地区仍只有通用政务入口，缺少精确教程路径和事项卡。
- 最终方案生成耗时 37-62 秒，演示体验偏慢。
- in-app browser 自动化点击“开始 AI 动态问诊”未触发状态变化，但控制台无错，需用户手动确认。

**当前进度**: 已完成多场景 QA，服务可用但内容准确性护栏还需继续加固。

**下一步**:
- 修复 `final-plan.ts`：按主体类型和 `companySubType` 过滤 DeepSeek 风险与文本段落。
- 修复 `adaptive-orchestrator.ts`：把行业选项 value 映射为中文标签和正确 `industryCategory`。
- 继续补北京、成都、深圳、厦门等地区官方事项卡。

---

## Session 11 — 修复 workspace 页面报错和恢复态卡死

**时间**: 2026-05-17 22:01 CST

**用户原始需求**:
- 用户反馈 `http://localhost:3000/workspace` 网页有报错，要求修好后让用户测试。
- 用户随后要求“继续”。

**解决的问题**:
- 确认当前目录仍不是 Git 仓库，`git log --oneline -3` 失败。
- 确认 3000 端口一度没有服务监听，已重新启动 `npm run dev`。
- 修复 `/workspace` 服务端首屏只显示“正在恢复自适应工作流...”的问题：
  - 首屏直接渲染默认工作台。
  - localStorage 旧会话只在客户端挂载后异步恢复。
  - 嵌入式浏览器缺少 `localStorage/navigator` 或客户端脚本未完整执行时，也不会只剩加载态。
- 修复旧 localStorage 缓存结构导致的运行时崩溃：
  - 归一化 `session/currentQuestion/history/stepResult/finalPlan`。
  - 缺失的 `location/address/roadmapSteps/materialsByStep/actionCards/materials` 等字段补安全默认值。
- 修复最终方案、路线图、材料分组、入口卡片和导出 payload 的防御式渲染，避免旧数据触发 `length/map/join` 读取错误。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/workspace/page.tsx | 首屏直接渲染工作台；新增旧缓存归一化；补齐数组字段防御渲染 |
| 修改 | MEMORY.md | 记录页面报错修复、验证证据和运行状态 |
| 修改 | 对话.md | 记录用户需求、处理过程、验证和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；有 Node `module.register()` 弃用警告，构建成功。
- `curl -I http://localhost:3000/workspace`：HTTP 200。
- Playwright 干净 localStorage：页面显示“AI 营业执照注册动态引导”和“开始 AI 动态问诊”。
- Playwright 旧缓存注入：`finalPlan` 缺 `roadmapSteps/materialsByStep` 时刷新不崩，旧方案仍可显示。
- in-app browser：`http://127.0.0.1:3000/workspace` 显示完整工作台，不再卡在“正在恢复自适应工作流...”，无 error/warning 日志。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：Next dev server 正在监听 3000 端口。

**当前进度**: `/workspace` 页面恢复正常，旧浏览器缓存兼容已加固，开发服务器已运行在 `http://localhost:3000`。

**下一步**:
- 用户可以直接刷新 `http://localhost:3000/workspace` 测试。
- 若想清空旧状态，从页面点击“重新开始”。
- 仍需初始化 Git 仓库并提交，满足比赛 GitHub 链接要求。

---

## Session 10 — 官方办理路线图与材料细目

**时间**: 2026-05-17 21:23 CST

**用户原始需求**:
- 用户指出当前方案没有清晰流程，不知道先办什么、后办什么、最后办什么。
- 用户要求每一步明确办理机构、官网入口、教程/依据、材料细目、去哪办和注意事项。
- 用户要求入口按钮真实跳转官网，同时显示可复制 URL。
- 用户要求材料详细到身份证复印件、地址证明、公司章程、住所证明等；但公司财产独立材料不能误写成设立登记必交。
- 用户要求继续只用 DeepSeek 和固定官网直连，不接 Tavily/Brave。

**解决的问题**:
- 新增最终方案主结构 `roadmapSteps` 和 `materialsByStep`。
- 本地护栏生成上海办理路线图，DeepSeek 只能补充相似步骤内容，不能重排、删除或插入不适用步骤。
- 公司路径按顺序覆盖名称申报、材料准备、设立登记、住所核验、实名认证/电子签名、审核领照、刻章、银行开户、税务、社保公积金、跨境电商附加事项。
- 个体户路径按顺序覆盖名称申报、材料准备、个体户设立、住所核验、实名认证/电子签名、审核领照、刻章、税务、社保公积金；过滤公司章程、股东出资和银行对公账户等公司专属内容。
- 一人有限公司财产独立材料作为 `risk_retention` 风险留存材料展示，非设立登记必交材料。
- `/workspace` 最终方案展示路线图卡片：第 N 步、办理机构、官网入口、官方教程、可见 URL、操作动作、阻塞条件、材料细目和下一步提示。
- “官方办理入口”卡片增加入口 URL 和依据 URL 可见文本。
- 修复 localStorage 导致的 hydration mismatch；导出模块改为按需动态加载，避免首屏加载重模块。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/lib/workflow/types.ts | 新增路线图、材料细目、官方来源类型 |
| 修改 | src/lib/workflow/guardrails.ts | 生成本地办理路线图和按步骤材料清单 |
| 修改 | src/lib/workflow/final-plan.ts | DeepSeek 最终方案 Prompt、白名单 URL、路线图合并和主体类型过滤 |
| 修改 | src/lib/workflow/regional-context.ts | 给官方来源和事项卡标注来源类型，保持固定官网直连 |
| 修改 | src/app/workspace/page.tsx | 最终方案路线图、按步骤材料、官网按钮、可见 URL、hydration 修复 |
| 修改 | src/components/features/export-actions.tsx | 导出工具按需动态导入，减少首屏依赖 |
| 修改 | MEMORY.md | 记录本轮架构决策和验证证据 |
| 修改 | 对话.md | 记录用户需求、处理过程、验证和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；有 Node `module.register()` 弃用警告，构建成功。
- `/api/ai/workflow-step`：上海公司住宅地址回答后返回 `success:true`，触发住宅地址风险，无搜索 Key 文案。
- `/api/ai/workflow-final-plan` 公司场景：上海浦东 + 一人有限公司 + 注册资本 120 万 + 住宅地址 + 跨境电商返回 11 步，包含银行、税务、社保和跨境电商附加事项；触发注册资本、一人公司、住宅地址风险；包含风险留存材料和住改商材料。
- `/api/ai/workflow-final-plan` 个体户场景：上海黄浦 + 电商网店 + 租赁地址返回 9 步；不包含公司章程、股东出资或公司银行对公账户步骤。
- Playwright 普通 Chromium 验收 `/workspace`：最终方案显示“办理路线图”“第 1 步”“打开官网”“官网 URL”“公司财产独立留存材料”“住改商/利害关系人同意材料”“跨境电商附加事项”；干净控制台无 Hydration failed、Error、Warning。

**当前进度**: `/workspace` 已从普通入口卡片升级为按官方办理顺序执行的注册路线图；上海公司/个体户主路径可演示。

**下一步**:
- 逐地补齐成都、厦门、深圳、北京等官方教程和精确事项卡。
- 增加真实用户问答路径的端到端脚本化验收，减少手工 localStorage 注入验证。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接要求。

---

## Session 9 — 细化官方办理入口和可执行操作指引

**时间**: 2026-05-17 20:18 CST

**用户原始需求**:
- 用户要求把 `/workspace` 里的“地区依据来源”和最终方案升级为可执行办事导航。
- 每个事项要展示官方入口、适用场景、点击路径、准备信息和注意事项。
- 继续只用 DeepSeek，不接 Tavily/Brave。
- 上海先做高细节样板，刻章、税务、银行开户等未确认精确入口时不能编造按钮。

**解决的问题**:
- 新增 `RegionalActionSource` 办理事项卡片结构。
- 上海内置完整办理事项资料包：名称自主申报、有限公司设立、个体户登记、住所登记、名称验证、刻章、税务、银行、社保/公积金、跨境电商、全国 2026 材料规范。
- DeepSeek 下一问和最终方案都接入 `actionCards`，并要求输出入口、点击路径、字段和提醒。
- `/workspace` 将“地区依据来源”改为“官方办理入口”，卡片可直接打开官方入口，并显示点击路径、准备信息和注意事项。
- 最终方案分段和导出内容也包含官方办理入口。
- 修复文本输入框缺少 `id/htmlFor` 导致浏览器自动化无法通过 label 填值的问题。
- 增强最终方案稳定性：DeepSeek JSON 输出上限提高到 8000 tokens；仅在 JSON 格式不稳定时使用本地护栏方案兜底。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/lib/workflow/types.ts | 新增 `RegionalActionSource`、`RegionalActionCategory` 和 `actionCards` 字段 |
| 修改 | src/lib/workflow/regional-context.ts | 内置上海高细节官方办理事项资料包和其他地区通用入口卡片 |
| 修改 | src/lib/workflow/adaptive-orchestrator.ts | 下一问 Prompt 要求 DeepSeek 返回办理事项卡片 |
| 修改 | src/lib/workflow/final-plan.ts | 最终方案 Prompt、归一化和兜底方案接入办理事项卡片 |
| 修改 | src/lib/workflow/guardrails.ts | 新增 `normalizeActionCards()`，本地护栏最终方案附入口卡片 |
| 修改 | src/lib/ai/deepseek-json.ts | 支持调用方设置 `maxTokens` |
| 修改 | src/app/workspace/page.tsx | 展示“官方办理入口”卡片、最终方案入口总览和导出内容；修复文本框 label |
| 修改 | MEMORY.md | 记录本次技术决策、验证证据和边界 |
| 修改 | 对话.md | 记录用户需求、处理过程、验证和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仍有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- 接口验证 `/api/ai/workflow-step`：上海 + 公司 + 跨境电商返回 `success:true`，包含 10 张 actionCards，含“上海有限公司设立登记”“上海名称自主申报”。
- 接口验证 `/api/ai/workflow-final-plan`：上海浦东 + 一人有限公司 + 注册资本 120 万 + 住宅地址 + 跨境电商返回 `success:true`，包含 10 张 actionCards，并触发本地风险兜底。
- 浏览器验证 `/workspace`：填“上海”后出现“上海有限公司设立登记”“上海名称自主申报”“打开入口”，无 `TAVILY_API_KEY` 提示。

**当前进度**: 上海样板的可执行官方办理入口已接入下一问、最终方案和 UI 展示。

**下一步**:
- 继续补齐成都、厦门、深圳、北京等地区的精确办理事项卡片。
- 给最终方案做更强的“按阶段筛选入口卡片”策略，减少部分步骤附带过多入口。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接要求。

---

## Session 8 — 改为只用 DeepSeek 生成地区化流程

**时间**: 2026-05-17 18:05 CST

**用户原始需求**:
- 用户看到 `/workspace` 仍提示缺少搜索 API Key。
- 用户明确要求不要使用 Tavily 或 Brave，只用 DeepSeek，让 DeepSeek 自己生成地区化内容。

**解决的问题**:
- `regional-context.ts` 不再调用外部搜索 API。
- 删除 Tavily/Brave 搜索调用路径，避免继续要求搜索 Key。
- `retrieveRegionalContext()` 只返回内置官方入口提示和市场监管总局 2026 规范提示。
- DeepSeek Prompt 改为基于自身知识和内置入口提示做地区化判断，并要求提示用户以官方最新页面为准。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/lib/workflow/regional-context.ts | 移除外部搜索依赖，仅保留内置地区上下文 |
| 修改 | src/lib/workflow/adaptive-orchestrator.ts | 调整 DeepSeek 下一问 Prompt |
| 修改 | src/lib/workflow/final-plan.ts | 调整最终方案 Prompt |
| 修改 | MEMORY.md | 记录本次修复和验证 |
| 修改 | 对话.md | 记录用户反馈与处理结果 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `curl /api/ai/workflow-step` 带回答进入下一步：返回 `success:true`，不再报缺搜索 Key。

**当前进度**: `/workspace` 自适应工作流现在只依赖 DeepSeek，不再要求搜索 API Key。

**下一步**:
- 在浏览器中继续走“上海 + 跨境电商 + 公司”路径，检查 DeepSeek 下一问是否足够地区化。

---

## Session 7 — 修复搜索 Provider 只认 Tavily

**时间**: 2026-05-17 17:50 CST

**用户原始需求**:
- 用户在 `/workspace` 看到缺少 `TAVILY_API_KEY` 的错误。
- 用户说明可以提供 Brave API Key，DeepSeek API Key 也已经提供，要求修复不要卡住。

**解决的问题**:
- `regional-context.ts` 从只支持 Tavily 改为支持 Tavily/Brave 双 Provider。
- 搜索 Provider 选择顺序：优先 `TAVILY_API_KEY`，否则使用 `BRAVE_API_KEY` / `BRAVE_SEARCH_API_KEY` / `BRAVE_SEARCH_KEY`。
- Brave Search 接入官方 Web Search API：`https://api.search.brave.com/res/v1/web/search`，header 使用 `X-Subscription-Token`。
- 移除 `workflow-step` 和 `workflow-final-plan` API 中硬编码 Tavily 的前置检查。
- 缺搜索 Key 时错误文案改成“配置 Tavily 或 Brave”，不再只卡 Tavily。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/lib/workflow/regional-context.ts | 新增 Brave Search Provider 和搜索 Key 自动选择 |
| 修改 | src/app/api/ai/workflow-step/route.ts | 移除 Tavily 专属前置检查 |
| 修改 | src/app/api/ai/workflow-final-plan/route.ts | 移除 Tavily 专属前置检查 |
| 修改 | MEMORY.md | 记录本次修复和验证 |
| 修改 | 对话.md | 记录用户反馈与处理结果 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `.env.local` 检查：当前只检测到 `DEEPSEEK_API_KEY`，未检测到搜索 Key。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `curl /api/ai/workflow-step` 带回答时，错误已改为缺少搜索 API Key，可配置 Tavily 或 Brave。

**当前进度**: 代码已支持 Brave Search Key，不再要求 Tavily。

**下一步**:
- 用户将 Brave Search Key 写入 `.env.local`，变量名建议 `BRAVE_API_KEY`。
- 重启 Next dev server 后验证“上海 + 跨境电商 + 公司”的真实联网动态问答。

---

## Session 6 — DeepSeek 自适应工作流重构

**时间**: 2026-05-17 17:30 CST

**用户原始需求**:
- 用户要求实现“DeepSeek 自适应”而不是写死决策树。
- 固定阶段 0-9 只作为护栏，下一问、地区差异、材料清单和最终方案必须根据用户回答动态变化。
- 当用户说“上海”时，系统应检索上海相关官方注册流程资料并喂给 DeepSeek。
- 本次要全链路改造，不只改下一问。

**解决的问题**:
- 新增 `src/lib/workflow/` 自适应工作流层，采用 `UserSession` 为核心状态。
- 新增地区实时检索层，要求 `TAVILY_API_KEY`；缺 Key 时明确失败，不伪装实时地区资料。
- 新增 DeepSeek 编排层，服务端把脱敏后的 `UserSession`、问答历史、地区检索结果和阶段护栏一起发给 DeepSeek。
- 新增 `/api/ai/workflow-step` 和 `/api/ai/workflow-final-plan`。
- `/workspace` 改为 DeepSeek 自适应动态问答界面，展示当前问题、依据来源、风险提示、材料缺口、最终方案和导出入口。
- 本地护栏兜底注册资本、一人有限公司、许可项目、住宅地址、虚拟地址、税务登记 30 天等风险。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 创建 | src/lib/workflow/types.ts | 自适应工作流类型定义 |
| 创建 | src/lib/workflow/guardrails.ts | 阶段护栏、风险规则、材料兜底、脱敏和免责声明 |
| 创建 | src/lib/workflow/regional-context.ts | Tavily 地区实时检索与官方入口提示 |
| 创建 | src/lib/workflow/adaptive-orchestrator.ts | DeepSeek 下一问编排 |
| 创建 | src/lib/workflow/final-plan.ts | DeepSeek 最终方案生成 |
| 创建 | src/app/api/ai/workflow-step/route.ts | 自适应下一问 API |
| 创建 | src/app/api/ai/workflow-final-plan/route.ts | 自适应最终方案 API |
| 重写 | src/app/workspace/page.tsx | DeepSeek 自适应动态问答工作台 |
| 修改 | MEMORY.md | 记录本次技术决策、验证证据和遗留风险 |
| 修改 | 对话.md | 记录用户原始需求与处理过程 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；仍有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- `curl /api/ai/workflow-step` 空会话：返回阶段 0 首问。
- `curl /api/ai/workflow-step` 带回答进入下一步：当前缺 `TAVILY_API_KEY` 时明确失败，提示实时地区检索不可用。
- `curl /api/ai/workflow-final-plan`：当前缺 `TAVILY_API_KEY` 时明确失败，不生成伪实时方案。

**当前进度**: DeepSeek 自适应工作流基础已落地，UI 和 API 已切换到新链路；缺搜索 Key 时会按用户要求失败提示，不再假装联网。

**下一步**:
- 配置 `TAVILY_API_KEY` 后验证“上海 + 跨境电商 + 公司”路径。
- 验证上海和成都同业务是否生成不同地区依据、不同后续问题和不同最终方案。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接证明要求。

---

## Session 1 — 项目初始化与规范建立

**时间**: 2025-05-17
**解决的问题**:
## Session 6 — `/workspace` 出海航线主题 UI 重构

**时间**: 2026-05-18

**用户原始需求**:
- 用户要求执行 `/workspace` 产品级 UI 重构计划，先安装并使用 GitHub UI 设计技能 `vercel-labs/agent-skills@web-design-guidelines`，把页面从技术工作台/卡片堆叠改为清晰、可信、可演示的产品界面。
- 用户中途明确纠偏：不要用“政务蓝”等视觉最大公约数设计，要基于跨境注册功能的主题元素构思，追求新颖创新。

**解决的问题**:
- 已安装 `web-design-guidelines` 技能，并读取 Web Interface Guidelines。
- `/workspace` 视觉主题从通用政务/后台风格改为“跨境出海注册航线”：
  - 注册流程 = 航线。
  - 问题 = 关卡。
  - 材料 = 舱单。
  - 官方入口 = 靠港入口。
  - 历史回答 = 航迹。
- 移除手账纸纹和红色边距线，改为浅绿灰航线网格背景。
- 页面重构为：顶部航线控制台、左侧“下一道关卡”主任务区、风险雷达/材料舱单状态条、出海办理路线图、右侧资料舱单和官方靠港入口。
- 官方入口卡增加可见 URL 和“复制链接”按钮，入口操作统一为“打开官网 / 查看依据 / 复制链接”。
- 可见 UI 不再暴露 `DeepSeek`、`Tavily`、`Brave`、`自适应主控`、`动态问诊`、`AI 营业执照`、`政务蓝` 等不适合产品展示的词。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/globals.css | 重设全局主题色和背景，移除纸纹/红线 |
| 修改 | src/app/workspace/page.tsx | 重构工作台布局、入口卡、路线图、状态栏、文案和复制链接 |
| 修改 | MEMORY.md | 记录本次 UI 主题决策、验证和遗留风险 |
| 修改 | 对话.md | 记录用户原始需求、纠偏和处理结果 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。
- in-app browser 打开 `http://127.0.0.1:3000/workspace`：首屏显示“出海注册航线 / 办理关卡 / 材料舱单 / 下一道关卡”，控制台无 warning/error。
- 浏览器冒烟测试：点击“重新开始”再点击“开始注册引导”，成功进入主体类型问题并显示主体选项。

**当前进度**: `/workspace` 已从普通技术工作台视觉改为主题化出海办理工作台，现有 DeepSeek 问答、官方入口、路线图和材料清单逻辑保持不变。

**下一步**:
- 继续修复业务准确性遗留问题：普通公司误触发一人有限公司风险、个体户文本混入公司事项、行业英文值映射。
- 初始化 Git 仓库，满足比赛 GitHub 链接证明要求。

---

- 建立 .qoder/rules/ 开发规范体系（4个规则文件）
- 创建 SDD 模板（SPEC/PLAN/TASKS 模板 + ARCHITECTURE 文档）
- 确定产品方向：跨境电商开店注册引导工具（聚焦亚马逊）
- 确定技术栈：Next.js 14 + TypeScript + Tailwind + shadcn/ui + DeepSeek API + Supabase
- 完成亚马逊开店流程深度调研
- 精简冗余文档（删除6个重复/方向错误的文件）
- 统一产品方向和AI模型配置

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 创建 | .qoder/rules/general.md | 通用开发规范 |
| 创建 | .qoder/rules/nextjs.md | Next.js 开发规范 |
| 创建 | .qoder/rules/ai-integration.md | AI集成规范 |
| 创建 | .qoder/rules/hackathon.md | 黑客松比赛规范 |
| 创建 | docs/SPEC-TEMPLATE.md | 产品规格说明模板 |
| 创建 | docs/TASKS-TEMPLATE.md | 任务分解模板 |
| 创建 | docs/PLAN-TEMPLATE.md | 开发计划模板 |
| 创建 | docs/ARCHITECTURE.md | 系统架构文档 |
| 创建 | AGENTS.md | Agent协作规范 |
| 创建 | MEMORY.md | 项目记忆文件 |
| 创建 | Agent开发工作流规范.md | 工作流规范 |
| 创建 | README.md | 项目说明 |
| 创建 | 开发日志.md | 本文件 |
| 删除 | docs/HACKATHON-BRIEF.md | 冗余 |
| 删除 | docs/QODER-AGENT-GUIDE.md | 冗余 |
| 删除 | docs/PROJECT-BLUEPRINT.md | 方向错误 |
| 删除 | 跨境电商黑客松技术架构.md | 方向错误 |
| 删除 | HANDOFF.md | 合并到开发日志 |
| 删除 | 对话.md | 合并到开发日志 |

**当前进度**: 文档体系建立完成，准备进入SPEC编写阶段
**下一步**: 编写正式 SPEC.md（产品需求规格说明书）

---

## Session 2 — 浏览器端导出辅助

**时间**: 2026-05-17
**解决的问题**:
- 按 Worker Agent 独立写入范围新增浏览器端报告/模板导出能力
- 支持复制文本、下载 Markdown、下载 HTML、触发浏览器打印 PDF
- 检测到项目已有 `docx` 依赖，已实现真实 DOCX 下载能力
- 未触碰 `src/types/domain.ts`、`src/stores/app-store.ts`、`src/lib/ai/*`、`src/app/api/*`、`src/app/page.tsx`、`src/app/workspace/page.tsx`

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 创建 | src/lib/export/workspace-export.ts | 工作台报告导出工具，包含文本、Markdown、HTML、PDF打印、DOCX导出 |
| 创建 | src/components/features/export-actions.tsx | 可接入页面的中文导出按钮组轻量组件 |
| 修改 | MEMORY.md | 记录本次技术决策、验证证据和范围约束 |
| 修改 | 开发日志.md | 记录本次Session进展 |

**验证步骤**:
- `npm run lint`：通过
- `npm run build`：通过；构建期间出现 Node `module.register()` 弃用警告，但不影响产物生成

**当前进度**: 导出辅助能力已完成，等待主控页面接入实际报告数据
**下一步**: 由主控在报告页或工作台页构造 `WorkspaceExportPayload` 并接入 `ExportActions`

---

## Session 3 — 重构为 AI 开店资料代办工作台

**时间**: 2026-05-17 15:25 CST

**用户原始需求**:
- 用户指出旧 MVP 很表面，只告诉大家准备营业执照、身份证等常识，没有真正节省时间。
- 用户要的是 AI 连续追问用户有没有公司、准备做什么、现有哪些资料，分析缺哪些资料，生成办照和注册模板。
- 用户办完营业执照后上传到本地，系统要记录统一社会信用代码等字段；最终生成 Amazon 后台所有需要填写的信息清单，方便复制粘贴。

**解决的问题**:
- 首页从“注册引导工具”改为“AI 开店资料代办工作台”入口。
- 新增 `/workspace` 单屏工作台，覆盖 AI 问答采集、办照信息、证照上传/粘贴解析、本地资料库、缺口分析、模板草稿、Amazon 填表包、导出交付物。
- 新增工作台领域模型：`ApplicationCase`、`FounderProfile`、`CompanyPlan`、`DocumentAsset`、`ExtractedDocument`、`GapAnalysis`、`GeneratedTemplate`、`AmazonFieldPacket` 等。
- 新增 5 个 AI API：追问、视觉证照字段抽取、缺口分析、模板草稿、Amazon 填表包；当时全部带 mock fallback，保证无 key 或 AI 失败时演示不断链，后续 Session 4 已改为严格联网失败提示 + 显式演示模式。
- 新增 IndexedDB 本地保存工具；Zustand store 扩展为工作台档案状态，并保留旧线性流程字段兼容旧页面。
- 接入 Worker Agent 的导出组件，支持复制、Markdown、HTML、DOCX、打印 PDF。
- 全局视觉调整为暖白纸张背景和红色边距线，保持浅色模式。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | package.json / package-lock.json | 安装 `docx` 依赖用于真实 DOCX 导出 |
| 修改 | src/types/domain.ts | 扩展工作台领域模型 |
| 修改 | src/types/api.ts | 扩展新 API 请求/响应类型 |
| 修改 | src/lib/ai/models.ts | 新增 GLM-4V 视觉模型配置 |
| 修改 | src/lib/ai/prompts.ts | 新增工作台、模板、填表包、证照 OCR prompt |
| 修改 | src/lib/ai/schemas.ts | 新增新 API 的 Zod 输出 Schema |
| 修改 | src/lib/ai/mock.ts | 新增工作台 mock、模板、OCR、填表包 fallback |
| 创建 | src/lib/api/response.ts | 统一 API 响应工具 |
| 创建 | src/lib/storage/indexed-db.ts | 浏览器本地 IndexedDB 保存工具 |
| 创建 | src/app/api/ai/next-question/route.ts | AI 下一步追问接口 |
| 创建 | src/app/api/ai/vision-extract/route.ts | GLM-4V/文本证照字段抽取接口 |
| 创建 | src/app/api/ai/gap-analysis/route.ts | 资料缺口分析接口 |
| 创建 | src/app/api/ai/template-draft/route.ts | 模板草稿生成接口 |
| 创建 | src/app/api/ai/amazon-packet/route.ts | Amazon 填表包生成接口 |
| 创建 | src/app/workspace/page.tsx | 主工作台页面 |
| 修改 | src/app/page.tsx | 首页入口改为工作台 |
| 修改 | src/app/globals.css | 暖白纸张背景、红色边距线、浅色视觉 |
| 创建 | src/lib/export/workspace-export.ts | 导出工具 |
| 创建 | src/components/features/export-actions.tsx | 导出按钮组 |
| 修改 | MEMORY.md | 记录本次技术决策、验证和风险 |
| 创建 | 对话.md | 记录本次用户诉求和处理过程 |

**验证步骤**:
- `npm run lint`：通过。
- `npm run build`：通过；有 Node `module.register()` 弃用警告和构建期 localStorage 实验警告，不影响构建。
- `curl /api/ai/gap-analysis`：成功返回缺口分析，当前走 fallback。
- `curl /api/ai/template-draft`：成功返回模板草稿，当前走 fallback。
- `curl /api/ai/vision-extract`：粘贴营业执照文字成功提取 `companyName`、`registrationNumber`、`legalRepresentative`、`registeredAddress`、`businessScope`。
- `curl -I /workspace`：HTTP 200。
- 浏览器手动演示：填写负责人和公司信息、粘贴营业执照文字、生成缺口分析、模板草稿和 Amazon 填表包，页面正常更新。

**当前进度**: 核心 MVP 已从浅层清单重构为资料代办工作台，能演示“问答采集 → 证照解析 → 本地资料记录 → 缺口分析 → 模板生成 → Amazon 填表包 → 导出”闭环。

**下一步**:
- 配置 `ZHIPU_API_KEY` 后用真实营业执照图片验证 GLM-4V OCR。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接证明要求。
- 轮换 `.env.local` 中已出现的真实形态 DeepSeek Key，避免泄露风险。

---

## Session 4 — 改成严格联网的 Agent 逐步问答工作台

**时间**: 2026-05-17 16:33 CST

**用户原始需求**:
- 用户在浏览器里验证后指出当前页面很杂乱，没有简洁逻辑链。
- 用户认为按钮触发 fallback 后像离线写死数据，不像 Agent 联网智能生成。
- 用户要求默认严格联网：失败就显示失败原因和重试，不要自动塞离线数据；离线 mock 只能作为显式演示模式。
- 用户要求首屏只保留当前问题、回答、AI 下一步建议和阶段进度，资料库/模板/填表包按流程逐步解锁。

**解决的问题**:
- `/workspace` 从一次性铺满模块改成分阶段状态机：现状判断 → 资料问诊 → 证照解析 → 缺口与模板 → 模板草稿 → Amazon 填表包。
- 首屏只问“有没有公司/营业执照”；若本地已有资料，先显示“继续上次资料包 / 新建资料包”。
- 新增 Agent 状态面板，展示 AI 正在依据哪些字段判断、已回答字段、缺失字段、当前阻塞点和问答历史。
- 主操作改成单一按钮，按钮文案随阶段变化；导出入口只在模板或填表包生成后出现。
- 严格联网模式下 AI API 失败不再自动 mock；显式开启演示模式后才允许 mock。
- 视觉 OCR 接口改为：图片无 `ZHIPU_API_KEY` 直接失败；粘贴文字可解析但标明为文本解析，不冒充图片 OCR。
- 扩展显式演示模式的追问链，避免两问就结束。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/workspace/page.tsx | 重写为 Agent 逐步问答工作台、单主按钮、阶段解锁和错误重试 UI |
| 修改 | src/stores/app-store.ts | 新增 `agentStage`、`currentQuestion`、`questionHistory`、`lastAiError`、`demoModeEnabled`、`startNewCase` |
| 修改 | src/types/domain.ts | 新增/保留 Agent 阶段与问答历史类型 |
| 修改 | src/types/api.ts | API 请求/响应补充 `demoMode` 和 `fallbackAvailable` |
| 修改 | src/lib/api/response.ts | `fail` 支持 `fallbackAvailable` |
| 修改 | src/app/api/ai/next-question/route.ts | 严格联网失败，不默认 mock |
| 修改 | src/app/api/ai/gap-analysis/route.ts | 严格联网失败，不默认 mock |
| 修改 | src/app/api/ai/template-draft/route.ts | 严格联网失败，不默认 mock |
| 修改 | src/app/api/ai/amazon-packet/route.ts | 严格联网失败，不默认 mock |
| 修改 | src/app/api/ai/vision-extract/route.ts | 图片 OCR 缺视觉 Key 时失败；粘贴文字返回 manual |
| 修改 | src/lib/ai/mock.ts | 显式演示模式追问链扩展 |
| 修改 | MEMORY.md | 记录严格联网、阶段化工作台和验证证据 |
| 修改 | 开发日志.md | 记录本次 Session |
| 修改 | 对话.md | 记录用户反馈、处理过程和遗留问题 |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run lint`：通过。
- `npm run build`：通过；有 Node `module.register()` 弃用警告和构建期 localStorage 实验警告，不影响构建。
- 无 Key API 验证：`gap-analysis`、`template-draft`、`amazon-packet`、`next-question` 返回 `success:false` 与 `fallbackAvailable:true`。
- 无视觉 Key 图片 OCR：`vision-extract` 返回缺少 `ZHIPU_API_KEY` 错误。
- 粘贴营业执照文字：`vision-extract` 返回 `source:"manual"` 并提取企业名称、统一社会信用代码、法定代表人、注册地址、经营范围。
- 浏览器验证 `http://localhost:3000/workspace`：本地历史资料先显示继续/新建；新建后只显示第一个问题；严格联网失败显示错误、重试和“开启演示模式并生成”；显式开启演示模式后才生成 mock 下一问。

**当前进度**: 工作台已从杂乱仪表盘改成严格联网优先的 Agent 逐步问答流程，默认不再用离线数据伪装成功。

**下一步**:
- 配置有效 `DEEPSEEK_API_KEY` 后验证真实联网追问、缺口分析、模板和填表包质量。
- 配置 `ZHIPU_API_KEY` 后用真实营业执照图片验证 GLM-4V OCR。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接证明要求。
- 轮换 `.env.local` 中曾出现的真实形态 DeepSeek Key，避免泄露风险。

---

## Session 5 — 修复 DeepSeek 已配置但下一问失败

**时间**: 2026-05-17 16:46 CST

**用户原始需求**:
- 用户看到页面提示“AI 下一问调用失败，严格联网模式下不自动使用离线追问”，指出已经提供 DeepSeek API Key，要求修复。

**解决的问题**:
- 确认 `.env.local` 中 `DEEPSEEK_API_KEY` 存在，且直接请求 DeepSeek 官方兼容接口成功返回 HTTP 200。
- 定位问题不是 Key，而是 AI SDK `generateObject` 结构化调用与当前 DeepSeek 返回格式兼容不稳定。
- 新增 DeepSeek 原生 JSON 请求工具，绕开 `generateObject`。
- 将 `next-question`、`gap-analysis`、`template-draft`、`amazon-packet` 改为 DeepSeek `/chat/completions` 原生 JSON 请求。
- 放宽下一问校验：`options` 缺失或非数组时默认 `[]`，`done` 缺失时默认 `false`，避免模型小格式波动导致失败。
- 补充前端对 AI 返回 `companyPlan.entityType` 的写入映射。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 创建 | src/lib/ai/deepseek-json.ts | DeepSeek 原生 JSON 请求与响应校验工具 |
| 修改 | src/app/api/ai/next-question/route.ts | 改用 DeepSeek 原生 JSON 请求 |
| 修改 | src/app/api/ai/gap-analysis/route.ts | 改用 DeepSeek 原生 JSON 请求 |
| 修改 | src/app/api/ai/template-draft/route.ts | 改用 DeepSeek 原生 JSON 请求 |
| 修改 | src/app/api/ai/amazon-packet/route.ts | 改用 DeepSeek 原生 JSON 请求 |
| 修改 | src/app/workspace/page.tsx | 支持写入 `companyPlan.entityType` |
| 修改 | MEMORY.md | 记录 Key 有效、失败根因和验证证据 |
| 修改 | 开发日志.md | 记录本次 Session |
| 修改 | 对话.md | 记录用户反馈与处理结果 |

**验证步骤**:
- `.env.local` 检查：`DEEPSEEK_API_KEY` 存在；未打印完整密钥。
- 直接请求 DeepSeek：`deepseek-chat` 和 `deepseek-reasoner` 均返回 HTTP 200。
- `curl /api/ai/next-question`：返回 `success:true`、`fallback:false`。
- 浏览器验证 `http://localhost:3000/workspace`：点击“重试”后显示“AI 下一问完成”，并出现真实下一问“您计划注册的公司名称是什么？”。
- `npm run lint`：通过。
- `npm run build`：通过；仍有既有 Node `module.register()` 弃用警告和 localStorage 实验警告。

**当前进度**: DeepSeek Key 已被正确使用，下一问真实联网恢复。

**下一步**:
- 继续用真实 Key 验证后续缺口分析、模板生成、Amazon 填表包在复杂档案下的稳定性。
- 配置 `ZHIPU_API_KEY` 后验证真实图片 OCR。

---

## Session 18 — Shopify-inspired workspace 任务台布局重构

**时间**: 2026-05-18 02:03 CST

**用户原始需求**:
- 用户要求基于 VoltAgent/awesome-design-md 的 Shopify Inspired DESIGN.md 重构 `/workspace` UI。
- 用户补充要求布局也要大改：重要内容前置，检验信息突出，次要内容支持收起/展开，避免像文章阅读。

**解决的问题**:
- 读取 Shopify Inspired DESIGN.md，并选择 transactional light track 作为视觉基准。
- 新增本地 `DESIGN.md`，沉淀本项目视觉规则。
- 将 `/workspace` 从上一版“出海航线”视觉改成商家开店任务台：cream canvas、white card、aloe/pistachio accent、black pill buttons。
- 压缩顶部，把首屏重点让给“当前开店决策”。
- 将官方入口、待补材料、已确认信息改为可展开面板。
- 将免责声明降级为可展开“办理前说明”。
- 最终方案内的材料分组、补充说明和官方入口也改为可展开结构。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | DESIGN.md | Shopify-inspired 规则项目化摘录 |
| 修改 | src/app/globals.css | 视觉 tokens、cream canvas、merchant card 组件类 |
| 修改 | src/components/ui/button.tsx | black pill / outline pill / aloe button 体系 |
| 修改 | src/app/workspace/page.tsx | 任务优先布局、折叠面板、文案去航线化、首屏重点前置 |
| 修改 | MEMORY.md | 记录本次 UI 重构和验证证据 |
| 修改 | 对话.md | 记录用户需求、处理过程和遗留问题 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告。
- in-app browser 打开 `http://127.0.0.1:3000/workspace`：首屏显示“当前开店决策”和主体类型问题。
- in-app browser 点击 `有限责任公司`：成功推进到省份问题，材料缺口更新。
- 页面正文检查无内部技术词和上一版航线隐喻。

**当前进度**: `/workspace` UI 已从文章式卡片流改为任务优先的商家开店工作台。

**下一步**:
- 修复业务准确性问题：普通公司误触发一人公司风险、个体户混公司事项、行业英文 value 映射错误。
- 初始化 Git 仓库并提交，满足比赛 GitHub 链接和开发过程证明要求。
- 补充产物：保存浏览器验收截图 `workspace-shopify-task-ui-20260518.png`。

---

## Session 19 — 启动 workspace 网页

**时间**: 2026-05-18 06:37 CST

**用户原始需求**:
- 用户要求启动网页。

**解决的问题**:
- 发现 3000 端口无服务监听。
- 启动当前项目的 Next dev server：`npm run dev -- --port 3000`。
- 验证 `/workspace` 可访问，并用 in-app browser 打开页面。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | MEMORY.md | 记录本次网页启动状态和验证证据 |
| 修改 | 对话.md | 记录用户需求和处理过程 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npm run dev -- --port 3000`：Next dev server 显示 `Ready in 1343ms`。
- `curl -I http://127.0.0.1:3000/workspace`：HTTP 200。
- `lsof -iTCP:3000 -sTCP:LISTEN -n -P`：node 监听 3000。
- in-app browser 打开 `http://localhost:3000/workspace`：页面显示“跨境开店注册助手”“当前开店决策”“开始注册引导”。

**当前进度**: `/workspace` 已启动，可在浏览器测试。

**下一步**:
- 继续修复业务准确性问题或按用户反馈调整 UI。

---

## Session 20 — 官网跳转与模板文件下载修复

**时间**: 2026-05-18 07:27 CST

**用户原始需求**:
- 用户要求实现 `/workspace` 官网跳转与模板下载修复：官网入口按钮必须能真实打开官方链接；最终方案生成后必须能下载材料清单、办理路线清单、公司章程草案、住所/经营场所证明草案、身份证复印件准备清单、Amazon 注册填表清单等文件。

**解决的问题**:
- 新增按场景生成模板文件的本地导出能力，不额外调用模型。
- 每个模板支持 DOCX、Markdown、HTML 下载。
- 新增全部模板 ZIP 打包下载。
- 公司场景生成公司章程草案；个体户场景不生成公司章程。
- 一人有限公司场景生成财产独立留存清单，并明确不是设立登记必交材料。
- 跨境电商场景生成 Amazon 注册填表清单和跨境电商后续事项清单。
- 官方入口统一为保留真实 `href` 的链接按钮，并提供 `window.open`、复制链接、当前页打开兜底。
- 清理用户可见的底层枚举和内部来源词。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 新增 | src/lib/export/template-export.ts | 场景化模板生成、单文件下载、ZIP 打包下载 |
| 修改 | src/lib/export/workspace-export.ts | 抽出 DOCX Blob、Blob 下载和文件名工具供模板复用 |
| 修改 | src/app/workspace/page.tsx | 官网入口按钮、模板下载区、模板下载按钮、UI 文案清洗 |
| 修改 | package.json | 新增 `jszip` 依赖 |
| 修改 | package-lock.json | 锁定 `jszip` 依赖 |
| 修改 | MEMORY.md | 记录本次技术变更和验证证据 |
| 修改 | 对话.md | 记录用户需求、处理过程和遗留边界 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和构建期 localStorage 实验警告。第一次 build 因 Google Fonts 临时请求失败，重试后成功。
- `tsx` 代码级场景验证：公司 + 一人有限公司 + 跨境电商会生成 `company-articles-draft`、`one-person-company-retention`、`amazon-registration-checklist`；个体户不会生成 `company-articles-draft`。
- in-app browser 验证 `http://localhost:3000/workspace`：首屏显示模板下载提示；选择有限责任公司并输入上海后，官方入口中的“打开官网/查看依据”为真实链接，包含 `https://yct.sh.gov.cn/portal_yct/`、上海指南 PDF、市场监管总局规范 URL。

**当前进度**: `/workspace` 已具备真实官网入口链接和模板文件下载能力。

**下一步**:
- 继续修复前序 QA 中的业务准确性问题：普通公司误触发一人公司风险、个体户混入公司事项、行业英文 value 写入状态。
- 若需要更完整浏览器验收，可走完整 DeepSeek 最终方案生成路径后实测 DOCX/ZIP 下载。

---

## Session 21 — 暖色文档办理工作台 UI 重构

**时间**: 2026-05-18 08:46 CST

**用户原始需求**:
- 用户要求按 Claude-inspired UI 重构计划改造 `/workspace`：从 Shopify 薄荷绿任务台改为暖奶油画布、珊瑚主操作、深色产品面板、编辑感标题、少阴影的文档办理工作台。
- 保留现有问答、官网入口、模板下载和最终方案逻辑，只重构视觉、布局和展示优先级。

**解决的问题**:
- 读取 `VoltAgent/awesome-design-md` 的 Claude 风格设计说明，抽象为本项目可用的视觉语言，不使用品牌名、logo 或官方资产。
- 将本地 `DESIGN.md` 替换为暖色文档办理工作台规则。
- 将全局视觉 token 从 Shopify 薄荷色改为暖奶油、珊瑚、深色产品面板和 hairline 边框。
- 将按钮体系从 black pill 改为 8px 圆角珊瑚 CTA、奶油 outline 和深色 secondary。
- 将 `/workspace` 首屏重排为左侧当前问题、右侧深色办理摘要；长入口、材料、历史信息下移并折叠。
- 将最终方案展示顺序调整为路线摘要、文件抽屉、路线图、材料分组。
- 保留官网入口真实 `href`、`data-official-url`、复制链接兜底，以及模板下载 `data-template-id`。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | DESIGN.md | 替换为暖色文档办理工作台设计规则 |
| 修改 | src/app/globals.css | 更新 Claude-inspired tokens、深色面板、cream panel、editorial heading |
| 修改 | src/components/ui/button.tsx | 更新按钮圆角、珊瑚主按钮、奶油 outline、深色 secondary |
| 修改 | src/app/workspace/page.tsx | 重排首屏、深色摘要面板、折叠信息区、最终方案展示顺序和文案 |
| 修改 | MEMORY.md | 记录本次 UI 重构和验证证据 |
| 修改 | 对话.md | 记录用户需求、处理过程和遗留说明 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- in-app browser 打开 `http://localhost:3000/workspace`：首屏显示暖奶油背景、珊瑚按钮、深色办理摘要面板。
- 点击“开始注册引导”后进入主体类型问题。
- DOM 检查：官方入口 `href` 和 `data-official-url` 均为真实官方链接。
- 页面正文检查无内部技术词、外部品牌词和上一版航线隐喻。

**当前进度**: `/workspace` 已完成暖色文档办理工作台 UI 改造，当前开发服务器仍在 3000 端口运行。

**下一步**:
- 继续修复业务准确性问题：普通公司误触发一人公司风险、个体户混入公司事项、行业英文 value 写入状态。
- 完整最终方案路径可继续实测 DOCX/ZIP 下载和上海公司跨境电商场景。

---

## Session 22 — React Bits 交互层接入

**时间**: 2026-05-18 09:47 CST

**用户原始需求**:
- 用户要求访问 `DavidHDev/react-bits`，并基于该项目为 `/workspace` 制作交互 UI。
- 用户指定使用 Chrome 和 in-app browser。

**解决的问题**:
- 用 Chrome 打开 React Bits GitHub 仓库，确认其定位为可复制、可定制的动画交互 React 组件集合。
- 本地只读克隆 React Bits，参考 `SpotlightCard`、`Magnet`、`TiltedCard`、`AnimatedContent` 等交互方向。
- 未直接安装 React Bits，未新增 `motion/react` 或 `gsap`，避免增加依赖和构建风险。
- 新增本地轻量交互：
  - `InteractiveSurface`：卡片跟随鼠标的 spotlight 和轻微 tilt。
  - `MagnetWrap`：按钮磁性位移。
  - `RevealOnView`：路线图步骤进入视口后逐步显现。
- 将交互应用到当前任务、深色摘要、风险/材料状态、侧栏档案、官网入口、模板文件抽屉、路线图和材料卡。
- 添加 `prefers-reduced-motion` 降级，减少动效敏感用户的负担。

**文件映射表**:
| 操作 | 文件路径 | 说明 |
|------|----------|------|
| 修改 | src/app/workspace/page.tsx | 新增本地交互组件并应用到工作台关键区块 |
| 修改 | src/app/globals.css | 新增 spotlight、magnet、reveal 动效和 reduced-motion 降级 |
| 修改 | MEMORY.md | 记录 React Bits 参考、实现方式和验证证据 |
| 修改 | 对话.md | 记录用户需求、处理过程和遗留说明 |
| 修改 | 开发日志.md | 记录本次 Session |

**验证步骤**:
- `git log --oneline -3`：失败，当前目录不是 Git 仓库。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run build`：通过；仅有既有 Node `module.register()` 弃用警告和 Next 静态生成期 localStorage 实验警告。
- in-app browser 打开 `http://localhost:3000/workspace`：页面存在 8 个 `.interactive-surface`、5 个 `.magnet-wrap`。
- 点击“开始注册引导”进入主体类型首问。
- DOM 检查：官方入口 `href` 与 `data-official-url` 仍是真实官方链接。
- 控制台无新增 error/warning。

**当前进度**: `/workspace` 已增加 React Bits 风格交互层，同时保留现有问答、官网入口和模板下载结构。

**下一步**:
- 在生成完整最终方案后补一轮路线图 `RevealOnView` 视觉验收。
- 继续修复业务准确性问题：普通公司误触发一人公司风险、个体户混入公司事项、行业英文 value 写入状态。
